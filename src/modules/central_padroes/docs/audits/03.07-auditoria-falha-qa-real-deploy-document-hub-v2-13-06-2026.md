# 🔴 Auditoria de Falha — Deploy QA Real — Document Hub V2 Pós-R5

**Data:** 13-06-2026  
**Série:** 03.07  
**Tipo:** Auditoria de falha operacional  
**Status:** 🔴 Deploy não atualizado no Netlify  
**Commit esperado:** `00d4ab8`  
**Commit publicado no Netlify:** `1ffe778`

---

## 1. Evidência visual

O painel Netlify mostra produção em:

```txt
Production: main@1ffe778 Published
12:00 PM: feat: central padroes document hub persistente
```

O commit esperado após push era:

```txt
00d4ab8 feat(central-padroes): habilita document hub v2 com persistencia e versionamento
```

Conclusão: **o deploy automático não promoveu o commit novo para produção**.

---

## 2. Verificações executadas

### 2.1 Git local

```powershell
git log --oneline -3
```

Resultado:

```txt
00d4ab8 feat(central-padroes): habilita document hub v2 com persistencia e versionamento
629cae4 feat(central-padroes): padronizacao documental completa - 52 arquivos renomeados para formato NN.NN
1ffe778 feat: central padroes document hub persistente
```

### 2.2 GitHub Actions manual

```powershell
gh workflow run netlify-deploy.yml --ref main
gh run view 27479480305 --json status,conclusion,headSha,displayTitle,url,createdAt,updatedAt
```

Resultado:

```json
{
  "conclusion": "success",
  "headSha": "00d4ab8156fad0ea419ce9cda4a0dbd5506869e5",
  "status": "completed",
  "url": "https://github.com/Dathex-Company/gpb-grupob-sagb/actions/runs/27479480305"
}
```

### 2.3 Log real do GitHub Actions

Trecho crítico:

```txt
Deploy production to Netlify (main)
env:
  NETLIFY_AUTH_TOKEN:
  NETLIFY_SITE_ID:
Netlify credentials not provided, not deployable
```

Conclusão: o workflow aparece como sucesso, mas **não fez deploy** porque os secrets estão vazios/ausentes.

### 2.4 Netlify CLI local

```powershell
netlify deploy --prod --dir=dist
```

Resultado:

```txt
JSONHTTPError: Forbidden
```

Conclusão: usuário/token local autenticado consegue ver o projeto, mas não tem permissão para criar deploy manual em produção.

---

## 3. Causa raiz

| Causa | Status |
|---|---|
| Commit não enviado ao GitHub | ❌ Não — commit foi enviado |
| Build quebrado | ❌ Não — build local e GitHub passam |
| Netlify CI automático funcionando | 🔴 Não |
| GitHub Actions sem secrets Netlify | 🔴 Sim |
| Netlify CLI local sem permissão deploy | 🔴 Sim |

**Causa raiz principal:** `NETLIFY_AUTH_TOKEN` e `NETLIFY_SITE_ID` não estão disponíveis no GitHub Actions.  
**Causa secundária:** Netlify CLI local retorna `Forbidden` para deploy manual.

---

## 4. Impacto

| Área | Impacto |
|---|---|
| Código no GitHub | 🟢 Atualizado |
| Supabase R5 | 🟢 Aplicado |
| Produção Netlify | 🔴 Ainda no commit antigo |
| QA real browser | 🔴 Bloqueado |
| Design System | 🟡 Aguardar deploy real ou confirmar override |

---

## 5. Correção recomendada

### Opção A — Corrigir GitHub Actions (preferida)

Adicionar/revalidar secrets no repositório GitHub:

```txt
NETLIFY_AUTH_TOKEN
NETLIFY_SITE_ID
VITE_SUPABASE_URL
VITE_SUPABASE_ANON_KEY
```

Depois disparar:

```powershell
gh workflow run netlify-deploy.yml --ref main
```

### Opção B — Corrigir permissões Netlify CLI

Reautenticar com usuário que tenha permissão de deploy no site `sagbapp`:

```powershell
netlify logout
netlify login
netlify deploy --prod --dir=dist
```

### Opção C — Usar botão manual Netlify

No painel Netlify:

```txt
Deploys → Trigger deploy → Deploy site
```

Ou confirmar se deploy automático GitHub está pausado/desconectado.

---

## 6. Status final

| Critério | Status |
|---|---|
| Commit criado | 🟢 `00d4ab8` |
| Push realizado | 🟢 |
| Build local | 🟢 |
| GitHub Actions build | 🟢 |
| Netlify deploy via Actions | 🔴 secrets ausentes |
| Netlify deploy via CLI | 🔴 Forbidden |
| Produção atualizada | 🔴 Não |
| QA real | 🔴 Bloqueado |

