# Plano 01.23 — Execução Editor Markdown Persistente — Document Hub V1

**Data:** 13-06-2026  
**Módulo:** `src/modules/central_padroes`  
**Origem:** Continuidade do arquivo `01.22-execucao-r5-persistencia-document-hub-v1.md`.  
**Escopo:** Editor markdown persistente integrado ao contrato R5 local.  
**Status:** concluído

---

## 1. Contexto Herdado

- Último arquivo lido: `01.22-execucao-r5-persistencia-document-hub-v1.md`.
- Último checkpoint concluído: C7 — persistência R5 local revisável.
- Checkpoint atual: C8 — editor markdown persistente.
- Próximo checkpoint esperado: C9 — fechamento, testes e QA.

---

## 2. Objetivo desta Etapa

Adicionar uma tela de edição de documento com markdown, preview, salvamento, upload opcional em storage, auditoria indireta via CRUD service e reindexação.

---

## 3. Arquivos Criados

| Arquivo | Finalidade |
|---|---|
| `src/modules/central_padroes/components/MarkdownEditor.tsx` | Editor markdown com abas Editar/Preview. |
| `src/modules/central_padroes/pages/DocumentoEditorPage.tsx` | Página de edição persistente de documento. |

---

## 4. Arquivos Alterados

| Arquivo | O que mudou |
|---|---|
| `src/modules/central_padroes/services/centralPadroesCrudService.ts` | `updateDocument()` passou a aceitar `summary`, `owner`, `tags` e `content`. |
| `src/modules/central_padroes/pages/DocumentoDetalhePage.tsx` | Adicionada ação “Editar markdown”. |
| `src/modules/central_padroes/layout/CentralPadroesLayout.tsx` | Adicionada view `documento-editor` e fluxo detalhe → editor → detalhe. |

---

## 5. Validação Executada

```text
npm run build
```

Resultado: build concluído com sucesso.

---

## 6. Critério de Pronto

- [x] Editor markdown criado.
- [x] Preview integrado.
- [x] Botão salvar chama persistência real via CRUD service.
- [x] Upload markdown para `cp-documents` é tentado quando há conteúdo.
- [x] Índice é invalidado após salvamento.
- [x] Permissões bloqueiam edição para perfis sem autorização.
- [x] Build passa.

---

## 7. Riscos Remanescentes

- Salvamento completo depende das migrations R5 estarem aplicadas no Supabase alvo.
- Upload real depende de sessão autenticada e policies do bucket.
- O build valida TypeScript, mas QA autenticado ainda precisa validar fluxo real por perfil.

---

## 8. Instrução para o Próximo Agente

Criar `01.24-fechamento-mega-tarefa-document-hub-v1.md` e relatório final em `src/modules/central_padroes/docs/reports/`, registrando todos os arquivos alterados, validação de build, riscos remanescentes e orientação para aplicar migrations R5 em ambiente controlado.
