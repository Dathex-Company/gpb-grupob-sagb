# Plano 03.01 â€” Plano R5 PersistÃªncia e Versionamento â€” Document Hub V2 â€” 13-06-2026

**Data:** 13-06-2026  
**Hora:** 13:15 (UTC-3)  
**MÃ³dulo:** `src/modules/central_padroes`  
**Origem:** Itens R5 pendentes do Document Hub V2 identificados no plano mestre [`03.00`](src/modules/central_padroes/docs/plans/03.00-plano-mestre-document-hub-v2-editor-oficiais-ativos-13-06-2026.md).  
**RestriÃ§Ã£o:** Este documento Ã© apenas plano. Nenhuma migration, policy, RPC, storage ou cÃ³digo-fonte serÃ¡ alterado sem autorizaÃ§Ã£o explÃ­cita.  
**Status:** plano

---

## 1. Itens R5 pendentes

| # | Item | Tabela/Alvo | Motivo |
|---|---|---|---|
| 1 | Coluna `official_status` | `central_padroes_documents` | Persistir classificaÃ§Ã£o oficial (oficial_ativo, rascunho, etc.) |
| 2 | Tabela `central_padroes_document_versions` | Nova tabela | Versionamento de conteÃºdo markdown |
| 3 | Tabela `central_padroes_document_drafts` | Nova tabela | Rascunhos separados do conteÃºdo oficial |
| 4 | Tabela `central_padroes_document_events` | Nova tabela | Auditoria editorial (quem publicou, quando, o que mudou) |
| 5 | RLS para novas tabelas | Policies | Mesmo padrÃ£o action-based do V1 |
| 6 | RPC `cp_publish_document` | Nova funÃ§Ã£o | PublicaÃ§Ã£o atÃ´mica: rascunho â†’ oficial + versÃ£o + evento |
| 7 | RPC `cp_restore_version` | Nova funÃ§Ã£o | Recuperar versÃ£o anterior |

---

## 2. Schema proposto

### 2.1 `central_padroes_documents` (ALTER)

```sql
ALTER TABLE public.central_padroes_documents
  ADD COLUMN IF NOT EXISTS official_status text,
  ADD COLUMN IF NOT EXISTS published_at timestamptz,
  ADD COLUMN IF NOT EXISTS published_by uuid REFERENCES auth.users(id);

CREATE INDEX IF NOT EXISTS idx_cp_documents_official_status
  ON public.central_padroes_documents(official_status);
```

### 2.2 `central_padroes_document_versions` (CREATE)

```sql
CREATE TABLE IF NOT EXISTS public.central_padroes_document_versions (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  document_id uuid NOT NULL REFERENCES public.central_padroes_documents(id) ON DELETE CASCADE,
  version integer NOT NULL,
  title text,
  content text,
  summary text,
  tags text[],
  official_status text,
  created_by uuid REFERENCES auth.users(id),
  created_at timestamptz DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_cp_doc_versions_document
  ON public.central_padroes_document_versions(document_id, version DESC);
```

### 2.3 `central_padroes_document_drafts` (CREATE)

```sql
CREATE TABLE IF NOT EXISTS public.central_padroes_document_drafts (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  document_id uuid NOT NULL REFERENCES public.central_padroes_documents(id) ON DELETE CASCADE,
  title text,
  content text,
  summary text,
  tags text[],
  owner text,
  updated_by uuid REFERENCES auth.users(id),
  updated_at timestamptz DEFAULT now(),
  UNIQUE(document_id)
);
```

### 2.4 `central_padroes_document_events` (CREATE)

```sql
CREATE TABLE IF NOT EXISTS public.central_padroes_document_events (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  document_id uuid NOT NULL REFERENCES public.central_padroes_documents(id) ON DELETE CASCADE,
  event_type text NOT NULL, -- 'draft_saved', 'published', 'version_restored', 'status_changed'
  previous_official_status text,
  new_official_status text,
  version_from integer,
  version_to integer,
  changed_by uuid REFERENCES auth.users(id),
  metadata jsonb DEFAULT '{}',
  created_at timestamptz DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_cp_doc_events_document
  ON public.central_padroes_document_events(document_id, created_at DESC);
```

---

## 3. RLS propsosto

Mesmo padrÃ£o do V1: policies por perfil (`editor`, `curador`, `aprovador`, `administrador`, `auditor`) usando `auth.jwt() -> 'app_metadata' ->> 'profile_role'`.

---

## 4. RPCs propostas

### 4.1 `cp_publish_document`

```sql
CREATE OR REPLACE FUNCTION public.cp_publish_document(
  p_document_id uuid,
  p_content text,
  p_title text DEFAULT NULL,
  p_summary text DEFAULT NULL,
  p_tags text[] DEFAULT NULL
) RETURNS jsonb
LANGUAGE plpgsql SECURITY DEFINER SET search_path = public
AS $$
-- 1. Ler versÃ£o atual
-- 2. Inserir em document_versions (version+1)
-- 3. Atualizar documents (official_status='oficial_ativo', published_at=now())
-- 4. Limpar draft
-- 5. Inserir event ('published')
-- 6. Retornar { version, document_id, status }
$$;
```

### 4.2 `cp_restore_version`

```sql
CREATE OR REPLACE FUNCTION public.cp_restore_document_version(
  p_document_id uuid,
  p_version integer
) RETURNS jsonb
LANGUAGE plpgsql SECURITY DEFINER SET search_path = public
AS $$
-- 1. Buscar versÃ£o alvo em document_versions
-- 2. Atualizar documents com conteÃºdo da versÃ£o
-- 3. Inserir event ('version_restored')
-- 4. Retornar { version, document_id }
$$;
```

---

## 5. Regras de seguranÃ§a

- âŒ NÃ£o executar sem autorizaÃ§Ã£o explÃ­cita
- âŒ NÃ£o fazer deploy
- âŒ NÃ£o alterar secrets
- âœ… Plano revisÃ¡vel localmente
- âœ… Rollback documentado
