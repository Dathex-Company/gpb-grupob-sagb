# Plano 01.22 — Execução R5 Persistência — Document Hub V1

**Data:** 13-06-2026  
**Módulo:** `src/modules/central_padroes`  
**Origem:** Continuidade do arquivo `01.21-autorizacao-r5-schema-rls-rpc-storage-document-hub-v1.md`.  
**Escopo:** Criação local revisável de migrations R5 e upload service para Document Hub V1.  
**Status:** concluído localmente

---

## 1. Contexto Herdado

- Último arquivo lido: `01.21-autorizacao-r5-schema-rls-rpc-storage-document-hub-v1.md`.
- Último checkpoint concluído: C6 — autorização/timestamp R5.
- Checkpoint atual: C7 — persistência local revisável.
- Próximo checkpoint esperado: C8 — editor markdown persistente.

---

## 2. Arquivos Criados

| Arquivo | Finalidade |
|---|---|
| `supabase/migrations/20260613000101_central_padroes_document_hub_v1_documents.sql` | Adiciona campos ricos e índices em `central_padroes_documents`. |
| `supabase/migrations/20260613000102_central_padroes_document_hub_v1_rls.sql` | Cria policies RLS action-based para documentos. |
| `supabase/migrations/20260613000103_central_padroes_document_hub_v1_ingest_rpc.sql` | Recria RPC `central_padroes_ingest_document` com contrato expandido. |

---

## 3. Arquivos Alterados

| Arquivo | O que mudou |
|---|---|
| `services/supabase.ts` | Exporta `supabaseConfig` e `getSupabaseAccessToken` para upload via Storage REST. |
| `src/modules/central_padroes/services/centralPadroesStorageService.ts` | Adiciona contrato expandido de ingestão e helpers de upload para `cp-documents` e `cp-evidence`. |

---

## 4. Validação Executada

```text
npm run build
```

Resultado: build concluído com sucesso. Não houve aplicação remota das migrations nesta etapa.

---

## 5. Critério de Pronto

- [x] Migration de schema rico criada.
- [x] Migration de RLS/policies criada.
- [x] Migration da RPC revisada criada.
- [x] Upload service usa buckets oficiais `cp-documents` e `cp-evidence`.
- [x] Path conventions implementadas no service.
- [x] Build passa.
- [x] Nenhum comando remoto de aplicação Supabase foi executado.

---

## 6. Riscos Remanescentes

- Policies RLS precisam ser testadas por perfil em Supabase real.
- RPC deve ser validada contra a estrutura exata da tabela `central_padroes_ingestion_queue` no ambiente remoto.
- Migrations locais ainda precisam ser aplicadas em ambiente controlado antes de confiar no editor persistente em produção.

---

## 7. Instrução para o Próximo Agente

Continuar pela Onda 6 criando `01.23-execucao-editor-markdown-persistente-document-hub-v1.md`. O editor deve salvar via `centralPadroesCrudService.updateDocument()` e opcionalmente enviar markdown para `cp-documents` usando `centralPadroesStorageService.uploadMarkdownDocument()`, com auditoria e reindexação.
