# Plano 01.18 Ã¢â‚¬â€ ExecuÃƒÂ§ÃƒÂ£o Onda 2 Detalhe e NavegaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ Document Hub V1

**Data:** 13-06-2026
**Hora:** 11:45 (UTC-3)
**MÃƒÂ³dulo:** `src/modules/central_padroes`
**Origem:** Continuidade do arquivo `01.17-execucao-onda-1-contrato-documental-document-hub-v1-13-06-2026.md`.  
**Escopo:** UI de leitura, detalhe documental e navegaÃƒÂ§ÃƒÂ£o real por documento, sem R5.  
**Status:** concluÃƒÂ­do

---

## 1. Contexto Herdado

- ÃƒÅ¡ltimo arquivo lido: `01.17-execucao-onda-1-contrato-documental-document-hub-v1-13-06-2026.md`.
- ÃƒÅ¡ltimo checkpoint concluÃƒÂ­do: C2 Ã¢â‚¬â€ contrato documental unificado.
- Checkpoint atual: C3 Ã¢â‚¬â€ navegaÃƒÂ§ÃƒÂ£o e detalhe documental.
- PrÃƒÂ³ximo checkpoint esperado: C4 Ã¢â‚¬â€ consistÃƒÂªncia entre listas, busca e Pietro.

---

## 2. Objetivo desta Etapa

Permitir abrir um documento especÃƒÂ­fico em tela de detalhe real, exibir metadados enriquecidos, caminhos, lacunas e preview markdown read-only.

---

## 3. Arquivos Alterados

| Arquivo | O que mudou |
|---|---|
| `src/modules/central_padroes/components/MarkdownPreview.tsx` | Criado preview markdown simples e seguro, com fallback para conteÃƒÂºdo ausente. |
| `src/modules/central_padroes/pages/DocumentoDetalhePage.tsx` | Criada tela de detalhe documental read-only. |
| `src/modules/central_padroes/pages/DocumentsPage.tsx` | Lista passou a consumir `centralPadroesDocumentHubService`, aceitar filtros iniciais e abrir documento por callback. |
| `src/modules/central_padroes/layout/CentralPadroesLayout.tsx` | Adicionada view `documento-detalhe`, estado `selectedDocumentId` e callback `openDocument`. |
| `src/modules/central_padroes/pages/SearchPage.tsx` | Preparada para abrir documentos por callback quando o resultado for documental. |
| `src/modules/central_padroes/pages/ChatPietroPage.tsx` | Preparada para abrir fontes documentais por callback. |
| `src/modules/central_padroes/pages/InternalDocsPage.tsx` | Wrapper agora aplica filtro/contexto e repassa callback. |
| `src/modules/central_padroes/pages/ExternalDocsPage.tsx` | Wrapper agora aplica filtro/contexto e repassa callback. |
| `src/modules/central_padroes/pages/ArchivePage.tsx` | Wrapper agora aplica contexto de arquivo/legado e repassa callback. |

---

## 4. ValidaÃƒÂ§ÃƒÂ£o Executada

```text
npm run build
```

Resultado: build concluÃƒÂ­do com sucesso. Permanecem apenas warnings existentes de chunk size/circular chunk/dynamic import.

---

## 5. CritÃƒÂ©rio de Pronto

- [x] Documento pode ser aberto por ID em detalhe real.
- [x] Detalhe exibe metadados ricos e lacunas.
- [x] Preview markdown existe com fallback seguro.
- [x] Layout possui estado para documento selecionado.
- [x] Busca e Pietro jÃƒÂ¡ tÃƒÂªm callback para abertura real.
- [x] NÃƒÂ£o foi criado editor persistente.
- [x] Nenhuma migration/RLS/RPC/storage remoto foi alterado.

---

## 6. PendÃƒÂªncias Herdadas

- Fazer busca textual consumir o DocumentHubService como fonte documental.
- Fazer ÃƒÂ­ndice/Pietro consumir DocumentHubService.
- Melhorar colunas/filtros avanÃƒÂ§ados da lista.
- Corrigir soft delete e auditoria documental em ondas posteriores.
- Criar pÃƒÂ¡gina real de triagem.

---

## 7. InstruÃƒÂ§ÃƒÂ£o para o PrÃƒÂ³ximo Agente

Continuar pela Onda 3 criando o prÃƒÂ³ximo arquivo sequencial `01.19-execucao-onda-3-busca-pietro-listas-document-hub-v1-13-06-2026.md`. A prÃƒÂ³xima implementaÃƒÂ§ÃƒÂ£o deve unificar listas, busca, ÃƒÂ­ndice e Chat Pietro sobre `centralPadroesDocumentHubService`, mantendo tudo sem R5.
