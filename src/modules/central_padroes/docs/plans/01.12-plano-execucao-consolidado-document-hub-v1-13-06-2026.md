# Plano 01.12 Ã¢â‚¬â€ ExecuÃƒÂ§ÃƒÂ£o Consolidada Document Hub V1 Ã¢â‚¬â€ Central de PadrÃƒÂµes

Data: 2026-06-13
Hora: 11:39 (UTC-3)
MÃƒÂ³dulo: `src/modules/central_padroes`
Escopo: consolidaÃƒÂ§ÃƒÂ£o dos planos `01.05`, `01.10` e `01.11`, com nova varredura do estado atual do mÃƒÂ³dulo.  
RestriÃƒÂ§ÃƒÂ£o aplicada: somente leitura, anÃƒÂ¡lise e documentaÃƒÂ§ÃƒÂ£o; nenhum arquivo de cÃƒÂ³digo-fonte foi modificado.

---

## 1. Resumo executivo

A Central de Documentos e PadrÃƒÂµes jÃƒÂ¡ possui uma base operacional relevante: layout prÃƒÂ³prio, sidebar ampla, dashboard, CRUDs principais, busca textual ampliada, Chat Pietro, serviÃƒÂ§os de ÃƒÂ­ndice, auditoria, permissÃƒÂµes, triagem, storage, governanÃƒÂ§a, relatÃƒÂ³rios, auditorias, curadoria, LOZE-TRACE e testes unitÃƒÂ¡rios para serviÃƒÂ§os centrais.

O Document Hub V1, porÃƒÂ©m, ainda nÃƒÂ£o estÃƒÂ¡ consolidado como hub mestre de documentos. O nÃƒÂºcleo documental atual continua baseado em uma entidade `CentralDocument` curta e na tabela `central_padroes_documents` com campos limitados. HÃƒÂ¡ ainda ausÃƒÂªncia de tela de detalhe documental, preview markdown, manifest documental enriquecido, editor persistente, normalizadores, filtros avanÃƒÂ§ados, navegaÃƒÂ§ÃƒÂ£o real para documentos individuais e reconciliaÃƒÂ§ÃƒÂ£o especÃƒÂ­fica entre `.md`, Supabase e registros de governanÃƒÂ§a.

A execuÃƒÂ§ÃƒÂ£o deve ser dividida em duas trilhas:

1. **Sem R5:** consolidar o Hub como camada de leitura, indexaÃƒÂ§ÃƒÂ£o, busca, detalhe, filtros, curadoria, auditoria e navegaÃƒÂ§ÃƒÂ£o, sem alterar schema/storage/RPC remoto.
2. **Com R5:** ampliar schema de `central_padroes_documents`, resolver storage/RPC/buckets, criar persistÃƒÂªncia real de conteÃƒÂºdo markdown e editor completo.

---

## 2. Resumo dos trÃƒÂªs planos base

### 2.1 Plano 01.05 Ã¢â‚¬â€ plano tÃƒÂ©cnico principal

O plano `01.05-plano-document-hub-v1-central-padroes-12-06-2026-deep-v4-pro.md` identifica o gap estrutural central: a tabela `central_padroes_documents` existe, mas nÃƒÂ£o possui campos suficientes para operar como Document Hub V1.

Pontos principais:

- transformar a Central de Ã¢â‚¬Å“ÃƒÂ­ndice + dashboard + CRUDs separadosÃ¢â‚¬Â em mÃƒÂ³dulo-mestre documental;
- expandir `CentralDocument` de entidade curta para entidade documental rica;
- criar manifest documental enriquecido;
- criar `DocumentoDetalhePage` e `MarkdownPreview`;
- refatorar `DocumentsPage` com filtros, colunas, aÃƒÂ§ÃƒÂµes e origem;
- integrar contadores de governance tables no dashboard;
- adicionar origem em resultados de busca;
- separar implementaÃƒÂ§ÃƒÂ£o sem R5 da implementaÃƒÂ§ÃƒÂ£o com R5;
- criar migration R5 para `central_padroes_documents` com `slug`, `type`, `risk_level`, `owner`, `tags`, `summary`, `content`, caminhos, origem, mÃƒÂ³dulo, divisÃƒÂ£o e autoria;
- criar `DocumentoEditorPage` apenas apÃƒÂ³s schema/persistÃƒÂªncia adequados.

### 2.2 Plano 01.10 Ã¢â‚¬â€ auditoria complementar sistÃƒÂªmica

O plano `01.10-plano-document-hub-v1-central-padroes-12-06-2026-codex-5-5.md` complementa o plano tÃƒÂ©cnico e detecta riscos que poderiam deixar o Hub visualmente funcional, mas sistemicamente inconsistente.

Pontos principais:

- jÃƒÂ¡ existe `centralPadroesIndexService.ts`; um novo manifest nÃƒÂ£o pode duplicar fonte de verdade;
- Chat Pietro usa ÃƒÂ­ndice antigo e rotas fictÃƒÂ­cias por item;
- `InternalDocsPage`, `ExternalDocsPage` e `ArchivePage` sÃƒÂ£o wrappers simples de `DocumentsPage`;
- hÃƒÂ¡ serviÃƒÂ§o real de triagem (`centralPadroesTriagemService.ts`) enquanto a UI de `ingestion` ainda ÃƒÂ© planned view;
- `centralPadroesStorageService.ts` usa nomes de buckets diferentes dos buckets oficiais da migration V5;
- auditoria documental nÃƒÂ£o estÃƒÂ¡ integrada a criar/editar/status/curadoria/delete;
- matriz de permissÃƒÂµes ÃƒÂ© centrada em padrÃƒÂµes, nÃƒÂ£o em documentos;
- status/tipos/risco precisam de normalizaÃƒÂ§ÃƒÂ£o;
- `deleteDocument()` usa DELETE real, apesar de haver soft delete no schema;
- reconciliaÃƒÂ§ÃƒÂ£o nÃƒÂ£o entende documentos fÃƒÂ­sicos `.md`, documentos vivos e governance records;
- Documentos Mestres precisam entrar no renderer comum do Hub;
- integraÃƒÂ§ÃƒÂµes TaskZei e Monitoramento estÃƒÂ£o mockadas/futuras;
- EvidencePage e Tags continuam planned views;
- faltam testes especÃƒÂ­ficos do Hub.

### 2.3 Plano 01.11 Ã¢â‚¬â€ comparativo e mÃƒÂ©todo de auditoria cascata

O relatÃƒÂ³rio `01.11-comparativo-planos-document-hub-v1-central-padroes-12-06-2026.md` compara as abordagens anteriores e propÃƒÂµe um padrÃƒÂ£o de auditoria profunda.

Pontos principais:

- a auditoria ideal precisa cruzar produto, inventÃƒÂ¡rio, dados, UI, serviÃƒÂ§os, navegaÃƒÂ§ÃƒÂ£o, busca/IA, persistÃƒÂªncia, governanÃƒÂ§a, integraÃƒÂ§ÃƒÂµes, testes e R5;
- a matriz entidade Ãƒâ€” camada ÃƒÂ© necessÃƒÂ¡ria para evitar lacunas;
- procurar Ã¢â‚¬Å“promessas falsasÃ¢â‚¬Â como `plannedView`, `return []`, `console.info`, `future`, `fallback`, rotas fictÃƒÂ­cias e storage divergente;
- seguir fluxos do clique ao banco: botÃƒÂ£o Ã¢â€ â€™ handler Ã¢â€ â€™ service Ã¢â€ â€™ tipo Ã¢â€ â€™ REST/RPC Ã¢â€ â€™ tabela/bucket Ã¢â€ â€™ RLS Ã¢â€ â€™ auditoria Ã¢â€ â€™ reindex Ã¢â€ â€™ reload;
- separar tudo em P0/P1/P2 sem R5 e R5 schema/storage/RLS/RPC.

---

## 3. Estado atual do mÃƒÂ³dulo

### 3.1 Entry points e contrato do mÃƒÂ³dulo

| Arquivo | Finalidade atual | ObservaÃƒÂ§ÃƒÂ£o Document Hub |
|---|---|---|
| `index.ts` | Exporta manifest, routes e module doc. | Barrel mÃƒÂ­nimo e adequado. |
| `manifest.ts` | Define id `central_padroes`, base route `/central_padroes`, owner Zico Padron. | Route usa underscore, enquanto alguns serviÃƒÂ§os geram `/central-padroes/...` com hÃƒÂ­fen. |
| `routes.tsx` | Registra `CentralPadroesLayout` em `/central_padroes/*`. | NÃƒÂ£o hÃƒÂ¡ rotas reais por item/documento. |
| `module-doc.ts` | Documenta propÃƒÂ³sito, limites e dependÃƒÂªncias de dados. | Buckets listados (`canonical-documents`, `evidence-files`) divergem dos buckets oficiais atuais (`cp-documents`, `cp-evidence`) e do service (`central-padroes-*`). |

### 3.2 Layout, navegaÃƒÂ§ÃƒÂ£o e sidebar

| Arquivo | Finalidade atual | ObservaÃƒÂ§ÃƒÂ£o Document Hub |
|---|---|---|
| `layout/CentralPadroesLayout.tsx` | Shell principal com sidebar, topbar, registrar, seed e renderizaÃƒÂ§ÃƒÂ£o por estado interno `currentView`. | NÃƒÂ£o possui view `documento-detalhe` nem estado de `selectedDocumentId`. |
| `data/sidebarConfig.ts` | Fonte da sidebar e breadcrumbs. | Inclui `tags`, `ingestion` e `evidence`, mas essas views ainda sÃƒÂ£o planned views no layout. |

Views planejadas detectadas:

- `tags`: classificaÃƒÂ§ÃƒÂ£o futura;
- `ingestion`: triagem visual planejada apesar de existir service real;
- `evidence`: evidÃƒÂªncias planejadas apesar de existir audit/evidence plumbing parcial e bucket oficial.

### 3.3 PÃƒÂ¡ginas documentais e de governanÃƒÂ§a

| Arquivo | Finalidade atual | Lacuna para Document Hub |
|---|---|---|
| `pages/DocumentsPage.tsx` | Lista documentos do snapshot, filtra por texto/status, cria/edita metadados bÃƒÂ¡sicos, chama ingestÃƒÂ£o via RPC ao criar. | NÃƒÂ£o mostra tipo, risco, owner real, tags, resumo, origem, caminhos absoluto/relativo, preview, detalhe ou aÃƒÂ§ÃƒÂµes de curadoria. |
| `pages/InternalDocsPage.tsx` | Wrapper de `DocumentsPage`. | Precisa filtro contextual interno. |
| `pages/ExternalDocsPage.tsx` | Wrapper de `DocumentsPage`. | Precisa filtro contextual externo. |
| `pages/ArchivePage.tsx` | Wrapper de `DocumentsPage`. | Precisa listar legado/arquivado/soft-deleted conforme permissÃƒÂ£o. |
| `pages/DocumentosMestresPage.tsx` | Lista standards `DM-*` de `documentacao_tecnica`, com detalhe inline e ediÃƒÂ§ÃƒÂ£o de metadados. | Deve usar renderer comum do Hub e nÃƒÂ£o ficar isolada. |
| `pages/DocumentoBasePage.tsx` | Mostra Documento-base 99 a partir do acervo/fallback. | Deve ser mapeado como documento especial no Hub. |
| `pages/SubdocumentosPrevistosPage.tsx` | Lista subdocumentos planejados. | Deve alimentar manifest/ÃƒÂ­ndice como previstos/incompletos. |
| `pages/CentralGovernanceRecordsPage.tsx` | CRUD genÃƒÂ©rico para reports/audits/curadoria com campos ricos. | JÃƒÂ¡ ÃƒÂ© referÃƒÂªncia de formulÃƒÂ¡rio rico para o Hub. |
| `pages/RelatoriosPage.tsx` | Wrapper governance para `central_padroes_reports`. | Operacional. |
| `pages/AuditsPage.tsx` | Wrapper governance para `central_padroes_audits`. | Operacional. |
| `pages/CuradoriaPage.tsx` | Wrapper governance para `central_padroes_curadoria`. | Operacional e deve receber aÃƒÂ§ÃƒÂµes do Hub. |
| `pages/AgentsPage.tsx` | Lista e cria LOZE-TRACE. | Operacional sobre `central_padroes_trace_logs`. |
| `pages/SearchPage.tsx` | Busca textual ampliada em snapshot + governance + trace. | NavegaÃƒÂ§ÃƒÂ£o por resultado volta ÃƒÂ  view geral, nÃƒÂ£o ao detalhe do item. |
| `pages/ChatPietroPage.tsx` | UI do Chat Pietro. | Ao clicar fonte dispara `sagb:navigate` com rota gerada, mas o layout atual nÃƒÂ£o processa navegaÃƒÂ§ÃƒÂ£o por item. |
| `pages/DashboardPage.tsx` | Cards de mÃƒÂ©tricas do snapshot e aprovaÃƒÂ§ÃƒÂµes. | Ainda nÃƒÂ£o inclui contadores de reports/audits/curadoria/trace. |

### 3.4 Tipos e modelo de domÃƒÂ­nio

| Arquivo | Estado atual | Lacuna para Document Hub |
|---|---|---|
| `types/index.ts` | Tipos ricos para standards, auditoria, permissÃƒÂµes, search, index e Chat Pietro. | `CentralDocument` permanece curto: `id`, `title`, `path`, `status`, `category`, `areaId`, `shouldBecome`, soft delete. |
| `types/index.ts` | `CentralAction` cobre aÃƒÂ§ÃƒÂµes de padrÃƒÂµes e agente. | NÃƒÂ£o cobre aÃƒÂ§ÃƒÂµes documentais como criar, editar metadados, editar conteÃƒÂºdo, publicar, arquivar, enviar para curadoria. |
| `types/index.ts` | `DocumentIndexEntry` cobre busca para Pietro. | NÃƒÂ£o inclui origem, path absoluto/relativo, contentAvailability, tipo documental real ou risk separado para documento. |
| `types/index.ts` | `SearchResult` jÃƒÂ¡ tem `originLabel` e `meta` rico. | Documentos do snapshot ainda nÃƒÂ£o preenchem meta rica porque o tipo/fonte ÃƒÂ© limitada. |

### 3.5 ServiÃƒÂ§os existentes

| ServiÃƒÂ§o | Finalidade atual | ObservaÃƒÂ§ÃƒÂ£o Document Hub |
|---|---|---|
| `centralPadroesRepository.ts` | Monta snapshot fallback + Supabase + governance_rules legadas. | Metrics ainda nÃƒÂ£o contam governance tables. |
| `centralPadroesSyncService.ts` | Busca online via CRUD service e monta snapshot. | SÃƒÂ³ inclui entidades clÃƒÂ¡ssicas; governance records entram pela busca, nÃƒÂ£o pelo snapshot. |
| `centralPadroesCrudService.ts` | CRUD de ÃƒÂ¡reas, standards, documents, decisions, checklists, modules e agents. | `mapDocument` e inputs sÃƒÂ³ cobrem campos antigos; `deleteDocument` usa DELETE real. |
| `centralPadroesIndexService.ts` | ConstrÃƒÂ³i ÃƒÂ­ndice em memÃƒÂ³ria para standards, documents e decisions, com cache e permissÃƒÂµes. | Rotas fictÃƒÂ­cias e documento limitado; deve virar consumidor do Hub/manifest. |
| `centralPadroesSearchService.ts` | Busca textual ampliada em snapshot, reports, audits, curadoria e trace logs. | Boa base, mas nÃƒÂ£o unificada com index/Pietro e sem detalhe por item. |
| `centralPadroesChatPietroService.ts` | Pergunta/resposta sobre ÃƒÂ­ndice e fallback textual. | Usa rotas fictÃƒÂ­cias e nÃƒÂ£o inclui governance tables na camada de ÃƒÂ­ndice. |
| `centralPadroesTriagemService.ts` | Lista fila de ingestÃƒÂ£o, aceita e ignora sugestÃƒÂµes. | Falta pÃƒÂ¡gina real de triagem. |
| `centralPadroesStorageService.ts` | Chama RPC `central_padroes_ingest_document` e declara buckets. | Buckets divergentes dos oficiais; nÃƒÂ£o faz upload em storage. |
| `centralPadroesAuditService.ts` | Loga eventos crÃƒÂ­ticos, owner, status, evidÃƒÂªncia, Chat Pietro e consultas. | NÃƒÂ£o estÃƒÂ¡ conectado ao CRUD documental. |
| `centralPadroesPermissionService.ts` | Resolve role e matriz de aÃƒÂ§ÃƒÂµes. | PermissÃƒÂµes documentais ausentes. |
| `centralPadroesReconciliationService.ts` | Compara fallback vs Supabase por entidade clÃƒÂ¡ssica. | NÃƒÂ£o reconcilia `.md` fÃƒÂ­sico, governance records, snapshots e documentos vivos. |
| `centralPadroesGovernanceService.ts` | CRUD genÃƒÂ©rico para reports/audits/curadoria e trace logs. | Base madura para registros ricos do Hub. |
| `centralPadroesReportsService.ts` | Wrapper de reports. | Operacional. |
| `centralPadroesAuditsService.ts` | Wrapper de audits. | Operacional. |
| `centralPadroesCuradoriaService.ts` | Wrapper de curadoria. | Operacional. |
| `centralPadroesTraceLogsService.ts` | Wrapper de LOZE-TRACE. | Operacional. |
| `centralPadroesSeedService.ts` | Sincroniza fallback para Supabase. | ÃƒÅ¡til, mas nÃƒÂ£o deve reconciliar automaticamente documentos fÃƒÂ­sicos sem confirmaÃƒÂ§ÃƒÂ£o. |
| `centralPadroesValidationService.ts` | Valida campos mÃƒÂ­nimos de CRUDs. | ValidaÃƒÂ§ÃƒÂ£o documental precisa expandir com campos do Hub. |

### 3.6 Componentes reutilizÃƒÂ¡veis

| Componente | Finalidade atual | Lacuna para Document Hub |
|---|---|---|
| `CentralPageShell.tsx` | Shell visual de pÃƒÂ¡gina. | ReutilizÃƒÂ¡vel. |
| `SectionPanel.tsx` | Painel de seÃƒÂ§ÃƒÂ£o. | ReutilizÃƒÂ¡vel. |
| `CrudModal.tsx` | Modal CRUD. | ReutilizÃƒÂ¡vel, mas editor documental precisa componentes especÃƒÂ­ficos. |
| `FormField.tsx` | Input/textarea simples. | Insuficiente para selects, tags, validaÃƒÂ§ÃƒÂ£o, disabled, hint, required e markdown. |
| `StatusBadge.tsx` | Badge para vÃƒÂ¡rios status/risco. | Precisa normalizadores e novos status documentais. |
| `Toast.tsx`, `ConfirmDialog.tsx`, `EmptyState.tsx`, `MetricCard.tsx` | UI de apoio. | ReutilizÃƒÂ¡veis. |
| `StandardTable.tsx`, `ChecklistPanel.tsx`, `AgentRunBoard.tsx`, `ApprovalBadge.tsx` | Componentes especÃƒÂ­ficos. | ÃƒÅ¡teis, mas nÃƒÂ£o substituem tabela/detail do Hub. |

Componentes ausentes para o Hub:

- `MarkdownPreview.tsx`;
- `MarkdownEditor.tsx` ou editor interno;
- `TagsField.tsx`;
- `SelectField.tsx`;
- `RiskSelect.tsx`;
- `StatusSelect.tsx`;
- `DocumentMetadataPanel.tsx` opcional;
- `DocumentAuditTimeline.tsx` opcional.

### 3.7 Docs, reports e audits existentes

O diretÃƒÂ³rio `docs/` jÃƒÂ¡ contÃƒÂ©m acervo significativo:

- `docs/plans/`: planos 01.00, 01.05, 01.10, 01.11 e planos anteriores de correÃƒÂ§ÃƒÂ£o, curadoria e UI/UX;
- `docs/reports/`: relatÃƒÂ³rios de fechamento, integraÃƒÂ§ÃƒÂ£o de busca/CRUDs, LOZE-TRACE, QA, reconciliaÃƒÂ§ÃƒÂ£o, dark mode e Supabase;
- `docs/audits/`: auditorias de uso real, UI/UX, dark mode, histÃƒÂ³rico Supabase, testes e comparaÃƒÂ§ÃƒÂ£o com TaskZei;
- `docs/overview/`: mapa documental e mapa de navegaÃƒÂ§ÃƒÂ£o;
- `docs/standards/`: padrÃƒÂµes LOZE e documentaÃƒÂ§ÃƒÂ£o guiada;
- `docs/MD-central-de-padroes/`: acervo de Documentos Mestres e fontes originais;
- `docs/99-curadoria/fora-do-padrao/`: materiais preservados fora do padrÃƒÂ£o.

Esse acervo deve ser inventariado pelo manifest do Hub, mas nÃƒÂ£o deve ser automaticamente Ã¢â‚¬Å“salvoÃ¢â‚¬Â como documento vivo no Supabase sem fluxo de triagem/curadoria.

### 3.8 Testes existentes

Testes encontrados:

- `__tests__/auditService.test.ts`;
- `__tests__/canonicalGateService.test.ts`;
- `__tests__/fase2_integration.test.ts`;
- `__tests__/permissionService.test.ts`;
- `__tests__/reconciliationService.test.ts`;
- `__tests__/types.test.ts`.

Lacuna: nÃƒÂ£o hÃƒÂ¡ testes especÃƒÂ­ficos para manifest documental, normalizadores, `DocumentsPage` parametrizada, wrappers documentais, detalhe documental, Chat Pietro com origem, busca com detalhe e permissÃƒÂµes documentais.

### 3.9 Supabase/migrations relacionados

Estado identificado nas migrations:

- `central_padroes_documents` foi criada com campos mÃƒÂ­nimos: `id`, `title`, `source_path`, `status`, `category`, `area_id`, `destination_type`, `created_at`, `updated_at`;
- migration V3 adicionou `deleted_at` e `deleted_by` e RLS de soft delete via update;
- existe tabela `central_padroes_ingestion_queue` e RPC `central_padroes_ingest_document`;
- migration V5 criou buckets oficiais `cp-documents` e `cp-evidence`;
- migration V5 criou `central_padroes_search_index` com pgvector/search_vector;
- migration 20260612000101 criou `central_padroes_reports`, `central_padroes_audits`, `central_padroes_trace_logs` e `central_padroes_curadoria` com campos ricos.

---

## 4. AnÃƒÂ¡lise de lacunas Ã¢â‚¬â€ existe vs precisa criar

### 4.1 NÃƒÂºcleo documental

| Capacidade | Existe hoje | NecessÃƒÂ¡rio no Document Hub V1 | ClassificaÃƒÂ§ÃƒÂ£o |
|---|---|---|---|
| Listar documentos | Sim, bÃƒÂ¡sico em `DocumentsPage`. | Lista enriquecida com tipo, risco, owner, tags, summary, origem e caminho. | Sem R5 para inferÃƒÂªncia; R5 para persistir. |
| Abrir detalhe | NÃƒÂ£o. | `DocumentoDetalhePage` com metadata panel, preview e aÃƒÂ§ÃƒÂµes. | Sem R5. |
| Preview markdown | NÃƒÂ£o. | `MarkdownPreview` com fallback seguro. | Sem R5. |
| Editar metadados ricos | Parcial, sÃƒÂ³ campos bÃƒÂ¡sicos. | Form completo com normalizaÃƒÂ§ÃƒÂ£o e permissÃƒÂµes. | Sem R5 parcial; R5 para persistir campos novos. |
| Editar conteÃƒÂºdo markdown | NÃƒÂ£o. | Editor com preview e salvamento. | R5. |
| Manifest `.md` | NÃƒÂ£o. | Manifest enriquecido alimentando Hub/index/search/Pietro. | Sem R5. |
| Documento vivo Supabase | Parcial. | Documento com `content`, `summary`, `tags`, `owner`, `source`, `canonical_level`. | R5. |
| ReconciliaÃƒÂ§ÃƒÂ£o `.md` Ã¢â€ â€ Supabase | NÃƒÂ£o. | RelatÃƒÂ³rio de divergÃƒÂªncia Document Hub. | Sem R5 para relatÃƒÂ³rio; R5 para aplicar merge persistente. |

### 4.2 Busca, ÃƒÂ­ndice e Chat Pietro

| Capacidade | Existe hoje | Lacuna |
|---|---|---|
| Busca textual global | Sim, em `centralPadroesSearchService`. | NÃƒÂ£o usa o mesmo ÃƒÂ­ndice do Pietro e documentos nÃƒÂ£o tÃƒÂªm metadados ricos. |
| ÃƒÂndice Pietro | Sim, em `centralPadroesIndexService`. | NÃƒÂ£o consome manifest e gera rotas por item inexistentes. |
| Chat Pietro | Sim, UI + service. | Fontes nÃƒÂ£o navegam corretamente para detalhe; governance records nÃƒÂ£o entram no ÃƒÂ­ndice principal. |
| Search index Supabase | Existe schema. | Frontend ainda usa fallback textual; pgvector/hÃƒÂ­brida fica futura. |

### 4.3 NavegaÃƒÂ§ÃƒÂ£o e UX

| Item | Existe hoje | Lacuna |
|---|---|---|
| Layout por view | Sim. | Falta seleÃƒÂ§ÃƒÂ£o por entidade e detail views. |
| Sidebar | Sim. | Entradas planned devem virar reais ou claramente limitadas. |
| Wrappers internos/externos/arquivo | Sim, wrappers. | Precisam filtros e contexto visual. |
| BotÃƒÂ£o abrir origem da busca | Sim. | Vai para view geral, nÃƒÂ£o item especÃƒÂ­fico. |
| Fonte do Chat Pietro | Sim. | Dispara evento com rota que o layout nÃƒÂ£o entende. |

### 4.4 GovernanÃƒÂ§a, seguranÃƒÂ§a e auditoria

| Item | Existe hoje | Lacuna |
|---|---|---|
| Auditoria | Service e tabela existem. | CRUD documental nÃƒÂ£o registra eventos. |
| PermissÃƒÂµes | Service e matriz existem. | AÃƒÂ§ÃƒÂµes documentais ausentes. |
| Soft delete | Schema e RLS existem. | Service ainda usa DELETE real. |
| Curadoria | PÃƒÂ¡gina e tabela existem. | AÃƒÂ§ÃƒÂµes do Hub nÃƒÂ£o enviam documento para curadoria com rastreabilidade. |
| Evidence | Plumbing parcial. | UI planned e buckets divergentes nos contratos. |

---

## 5. Faseamento proposto

### Fase P0-A Ã¢â‚¬â€ Contrato documental unificado sem R5

DependÃƒÂªncias: nenhuma.

- [ ] Criar `services/centralPadroesDocumentHubService.ts` como fachada ÃƒÂºnica para documentos do Hub.
- [ ] Criar `data/centralDocumentsManifest.ts` com inventÃƒÂ¡rio estÃƒÂ¡tico/`import.meta.glob` dos `.md` relevantes.
- [ ] Definir tipo documental enriquecido com campos opcionais em `CentralDocument`.
- [ ] Criar normalizadores `normalizeDocumentStatus`, `normalizeDocumentType`, `normalizeRiskLevel`.
- [ ] Criar mapeamento de origem: `supabase_live`, `md_indexado`, `governance_report`, `governance_audit`, `governance_curadoria`, `trace_log`, `fallback`.
- [ ] Garantir que o manifest alimente o DocumentHubService e que o IndexService consuma essa fachada, nÃƒÂ£o um ÃƒÂ­ndice paralelo.

### Fase P0-B Ã¢â‚¬â€ Tela de detalhe e preview sem R5

DependÃƒÂªncias: P0-A.

- [ ] Criar `components/MarkdownPreview.tsx`.
- [ ] Criar `pages/DocumentoDetalhePage.tsx`.
- [ ] Exibir metadados ricos: tÃƒÂ­tulo, tipo, status normalizado, risco, owner, tags, categoria, caminhos, origem, mÃƒÂ³dulo, divisÃƒÂ£o, nÃƒÂ­vel canÃƒÂ´nico e disponibilidade de conteÃƒÂºdo.
- [ ] Exibir preview quando `content` existir e fallback quando conteÃƒÂºdo nÃƒÂ£o estiver disponÃƒÂ­vel.
- [ ] Adicionar aÃƒÂ§ÃƒÂµes seguras: copiar caminho, marcar incompleto localmente/metadata quando possÃƒÂ­vel, enviar para curadoria, voltar para lista.
- [ ] Exibir timeline de auditoria quando houver logs e fallback silencioso quando audit log falhar.

### Fase P0-C Ã¢â‚¬â€ NavegaÃƒÂ§ÃƒÂ£o interna por documento

DependÃƒÂªncias: P0-B.

- [ ] Expandir `CentralPadroesView` com `documento-detalhe`.
- [ ] Adicionar estado `selectedDocumentId`/`selectedDocumentRef` no layout.
- [ ] Passar callback `onOpenDocument` para `DocumentsPage`, `SearchPage` e Chat Pietro.
- [ ] Padronizar rotas internas geradas por services para view/action do layout, evitando `/central-padroes/documents/{id}` fictÃƒÂ­cio.
- [ ] Fazer clique em busca e fontes do Pietro abrir detalhe real.

### Fase P0-D Ã¢â‚¬â€ Refatorar lista documental e wrappers

DependÃƒÂªncias: P0-A e P0-C.

- [ ] Parametrizar `DocumentsPage` com `initialFilters`, `title`, `subtitle`, `context` e `sourceScope`.
- [ ] Adicionar colunas: tipo, risco, owner, tags, origem, caminho.
- [ ] Adicionar filtros: tipo, risco, owner, tag, origem e status normalizado.
- [ ] Adicionar aÃƒÂ§ÃƒÂ£o Ã¢â‚¬Å“AbrirÃ¢â‚¬Â por linha.
- [ ] Transformar `InternalDocsPage` em filtro de internos.
- [ ] Transformar `ExternalDocsPage` em filtro de externos.
- [ ] Transformar `ArchivePage` em filtro de legado/arquivado/soft-deleted quando permitido.
- [ ] Integrar `DocumentosMestresPage`, `DocumentoBasePage` e `SubdocumentosPrevistosPage` ao renderer/detalhe comum sem duplicar entidade.

### Fase P0-E Ã¢â‚¬â€ Busca, ÃƒÂ­ndice e Pietro consistentes

DependÃƒÂªncias: P0-A e P0-C.

- [ ] Atualizar `centralPadroesIndexService.ts` para consumir DocumentHubService.
- [ ] Expandir `DocumentIndexEntry` com origem, paths, risk, tags ricas e contentAvailability.
- [ ] Atualizar `centralPadroesSearchService.ts` para usar metadados do Hub em documentos e manter governance records.
- [ ] Atualizar `centralPadroesChatPietroService.ts` para usar o ÃƒÂ­ndice unificado e incluir origem/canonicidade nas fontes.
- [ ] Atualizar `ChatPietroPage` para abrir detalhe por callback/evento entendido pelo layout.

### Fase P1-A Ã¢â‚¬â€ Triagem real e curadoria operacional

DependÃƒÂªncias: P0-A.

- [ ] Criar `pages/TriagemIngestaoPage.tsx` ou `pages/IngestionPage.tsx`.
- [ ] Conectar a `centralPadroesTriagemService.listQueue`, `acceptSuggestion` e `ignore`.
- [ ] Ao aceitar item, abrir criaÃƒÂ§ÃƒÂ£o/ediÃƒÂ§ÃƒÂ£o de documento ou enviar para curadoria.
- [ ] Registrar evento de auditoria de triagem.
- [ ] Substituir `plannedView` de `ingestion` por pÃƒÂ¡gina real.

### Fase P1-B Ã¢â‚¬â€ Auditoria, permissÃƒÂµes e soft delete documental

DependÃƒÂªncias: P0-A e P0-D.

- [ ] Expandir `CentralAction` com aÃƒÂ§ÃƒÂµes documentais.
- [ ] Atualizar `CENTRAL_PERMISSION_MATRIX` para documentos.
- [ ] Adicionar helpers `canEditDocument`, `canPublishDocument`, `canArchiveDocument`, `canSendDocumentToCuradoria`.
- [ ] Integrar `centralPadroesAuditService` em create/update/status/curadoria/delete/restore/copy path quando aplicÃƒÂ¡vel.
- [ ] Trocar `deleteDocument()` para soft delete via `deleted_at`/`deleted_by`.
- [ ] Exibir botÃƒÂµes desabilitados e estado somente leitura conforme perfil.

### Fase P1-C Ã¢â‚¬â€ Dashboard e governanÃƒÂ§a cruzada

DependÃƒÂªncias: P0-A.

- [ ] Expandir `CentralDashboardMetrics` com `reports`, `audits`, `curadoria`, `traceLogs`, `documentsIncomplete`, `documentsWithoutOwner`.
- [ ] Atualizar `centralPadroesRepository.getMetrics()` ou criar metrics service dedicado.
- [ ] Adicionar MetricCards no dashboard para RelatÃƒÂ³rios, Auditorias, Curadoria e LOZE-TRACE.
- [ ] Adicionar seÃƒÂ§ÃƒÂ£o de lacunas documentais: sem owner, sem tag, sem resumo, sem path, sem conteÃƒÂºdo, duplicados.

### Fase P1-D Ã¢â‚¬â€ ReconciliaÃƒÂ§ÃƒÂ£o Document Hub

DependÃƒÂªncias: P0-A.

- [ ] Atualizar `centralPadroesReconciliationService.ts` para reconhecer `md_indexado`, `supabase_live`, `governance_record` e `fallback`.
- [ ] Gerar relatÃƒÂ³rio especÃƒÂ­fico de divergÃƒÂªncias Document Hub.
- [ ] Marcar `.md` sem registro Supabase e Supabase sem `.md` correspondente.
- [ ] NÃƒÂ£o aplicar seed/merge automÃƒÂ¡tico sem confirmaÃƒÂ§ÃƒÂ£o explÃƒÂ­cita.

### Fase P2-A Ã¢â‚¬â€ Evidence, tags e integraÃƒÂ§ÃƒÂµes com transparÃƒÂªncia operacional

DependÃƒÂªncias: P1-B e decisÃƒÂ£o de storage.

- [ ] Criar EvidencePage mÃƒÂ­nima ou manter planned view com mensagem de limitaÃƒÂ§ÃƒÂ£o real.
- [ ] Criar TagsPage mÃƒÂ­nima ou incorporar tags como filtro/gestÃƒÂ£o no Hub.
- [ ] Marcar TaskZei e Monitoramento como nÃƒÂ£o operacionais quando retornarem mock/`return []`.
- [ ] Remover ou bloquear botÃƒÂµes que prometem integraÃƒÂ§ÃƒÂ£o real sem persistÃƒÂªncia.

### Fase P2-B Ã¢â‚¬â€ Testes e QA do Hub

DependÃƒÂªncias: P0/P1 correspondentes.

- [ ] Testar manifest/enriquecimento.
- [ ] Testar normalizadores.
- [ ] Testar DocumentHubService.
- [ ] Testar IndexService com documentos enriquecidos.
- [ ] Testar SearchService com origem e navegaÃƒÂ§ÃƒÂ£o.
- [ ] Testar Chat Pietro retornando documentos, reports, audits e curadoria.
- [ ] Testar permissÃƒÂµes documentais.
- [ ] Testar wrappers `InternalDocsPage`, `ExternalDocsPage`, `ArchivePage`.
- [ ] Executar build/test apÃƒÂ³s blocos de implementaÃƒÂ§ÃƒÂ£o.

---

## 6. Itens que requerem R5

Os itens abaixo exigem alteraÃƒÂ§ÃƒÂ£o de Supabase remoto, schema, storage, RLS/policies ou RPC e devem ficar isolados em plano prÃƒÂ³prio.

| Item | Tipo R5 | Motivo |
|---|---|---|
| Adicionar campos ricos em `central_padroes_documents` | Schema | Persistir `slug`, `type`, `risk_level`, `owner`, `tags`, `summary`, `content`, paths, origem, mÃƒÂ³dulo, divisÃƒÂ£o, canonicalidade e autoria. |
| ÃƒÂndices em campos novos | Schema/performance | Filtros por tipo, risco, owner, origem, tags e canonicalidade. |
| Ajustar RLS para campos e aÃƒÂ§ÃƒÂµes documentais | RLS/policy | Garantir leitura, ediÃƒÂ§ÃƒÂ£o, publicaÃƒÂ§ÃƒÂ£o, arquivamento e conteÃƒÂºdo por perfil. |
| Trocar delete real por soft delete obrigatÃƒÂ³rio | Schema/RLS/service | Schema jÃƒÂ¡ tem soft delete, mas polÃƒÂ­ticas e service precisam convergir. |
| Padronizar buckets | Storage | Service usa `central-padroes-documents`, `central-padroes-evidence`, `central-padroes-templates`; migration criou `cp-documents`, `cp-evidence`; module-doc cita outros nomes. |
| Implementar upload real de documento/evidÃƒÂªncia | Storage/RLS | Hoje `centralPadroesStorageService` sÃƒÂ³ chama RPC, nÃƒÂ£o faz upload em bucket. |
| Revisar/estender `central_padroes_ingest_document` | RPC | RPC existe, mas precisa confirmar contrato com Hub, storage, raw content, triagem, auditoria e retorno estruturado. |
| Usar `central_padroes_search_index`/pgvector operacionalmente | Schema/RPC/job | Tabela existe, mas frontend ainda usa busca textual. Reindex semÃƒÂ¢ntico exigiria pipeline/RPC/job. |
| Persistir conteÃƒÂºdo markdown editado | Schema/storage | Requer `content` ou storage path oficial. |
| Editor documental completo | Depende de schema/storage | UI pode existir sem salvar conteÃƒÂºdo, mas salvamento real ÃƒÂ© R5. |

Plano R5 correspondente: `01.13-plano-r5-document-hub-13-06-2026.md`.

---

## 7. Riscos e bloqueios conhecidos

| Risco/bloqueio | Impacto | MitigaÃƒÂ§ÃƒÂ£o |
|---|---|---|
| Duplicar manifest e index service | Busca/Pietro inconsistentes. | Criar DocumentHubService como fonte ÃƒÂºnica e fazer IndexService consumir a fachada. |
| Rotas fictÃƒÂ­cias `/central-padroes/...` | Fontes do Pietro e busca nÃƒÂ£o abrem item real. | NavegaÃƒÂ§ÃƒÂ£o por estado interno e callbacks do layout. |
| Schema limitado de `central_padroes_documents` | NÃƒÂ£o persiste conteÃƒÂºdo/metadados ricos. | Fazer Hub sem R5 com campos inferidos e separar persistÃƒÂªncia no plano R5. |
| Buckets divergentes | Upload/ingestÃƒÂ£o falham. | Padronizar nomes em R5 antes de prometer storage. |
| DELETE real em documentos | Perda de histÃƒÂ³rico ou falha por RLS. | Migrar service para soft delete e auditoria. |
| PermissÃƒÂµes documentais ausentes | Leitores podem ver aÃƒÂ§ÃƒÂµes indevidas ou editors podem salvar indevidamente. | Expandir matriz e bloquear UI/service. |
| Planned views parecem prontas | UsuÃƒÂ¡rio encontra telas sem operaÃƒÂ§ÃƒÂ£o real. | Transformar triagem em real; evidÃƒÂªncias/tags com aviso explÃƒÂ­cito ou implementaÃƒÂ§ÃƒÂ£o mÃƒÂ­nima. |
| Adapters mockados | IntegraÃƒÂ§ÃƒÂ£o aparente sem efeito real. | Sinalizar nÃƒÂ£o operacional e remover promessas de automaÃƒÂ§ÃƒÂ£o. |
| Documentos `.md` sem metadados | Lista fica incompleta/confusa. | Marcar como incompleto e permitir curadoria. |
| Busca textual vs pgvector | Expectativa de IA sem semÃƒÂ¢ntica real. | Manter rÃƒÂ³tulo Ã¢â‚¬Å“textualÃ¢â‚¬Â atÃƒÂ© pipeline semÃƒÂ¢ntico R5/posterior. |
| Editor sem R5 | Falso salvamento de markdown. | NÃƒÂ£o criar botÃƒÂ£o de salvar conteÃƒÂºdo real antes da persistÃƒÂªncia. |

---

## 8. Mermaid Ã¢â‚¬â€ arquitetura consolidada

```mermaid
flowchart TD
  A[Docs md do modulo] --> B[centralDocumentsManifest]
  C[Supabase documents] --> D[DocumentHubService]
  E[Governance records] --> D
  B --> D
  D --> F[DocumentsPage filtrada]
  D --> G[DocumentoDetalhePage]
  D --> H[SearchService]
  D --> I[IndexService]
  I --> J[Chat Pietro]
  G --> K[AuditService]
  G --> L[Curadoria]
  M[Triagem queue] --> N[IngestionPage]
  N --> D
```

---

## 9. Ordem consolidada de execuÃƒÂ§ÃƒÂ£o

1. Implementar contrato do Hub sem R5: tipos opcionais, normalizadores, manifest e DocumentHubService.
2. Conectar IndexService, SearchService e Chat Pietro ao contrato unificado.
3. Criar detalhe documental e preview markdown read-only.
4. Ajustar layout para abrir documento especÃƒÂ­fico.
5. Refatorar DocumentsPage e wrappers internos/externos/arquivo.
6. Integrar Documentos Mestres, Documento-base 99 e Subdocumentos Previstos ao renderer comum.
7. Criar Triagem/IngestionPage real.
8. Adicionar auditoria e permissÃƒÂµes documentais.
9. Corrigir soft delete documental no service.
10. Expandir dashboard com governance counts e lacunas documentais.
11. Atualizar reconciliaÃƒÂ§ÃƒÂ£o para Document Hub.
12. Adicionar testes unitÃƒÂ¡rios e QA.
13. Executar plano R5 separado para schema/storage/RPC antes de editor persistente.

---

## 10. CritÃƒÂ©rios de pronto do Document Hub V1 sem R5

- [ ] Documentos Supabase, `.md` indexados e registros de governanÃƒÂ§a aparecem em visÃƒÂ£o unificada ou claramente segmentada.
- [ ] Cada documento pode abrir tela de detalhe real.
- [ ] Metadados ricos sÃƒÂ£o exibidos, mesmo quando inferidos.
- [ ] Documentos incompletos sÃƒÂ£o marcados como incompletos, nÃƒÂ£o ocultados.
- [ ] Preview markdown funciona quando conteÃƒÂºdo estiver disponÃƒÂ­vel.
- [ ] Nenhum fluxo promete salvar markdown local em `Z:\`.
- [ ] Busca e Chat Pietro usam fonte compatÃƒÂ­vel e abrem item real.
- [ ] Wrappers internos/externos/arquivo tÃƒÂªm filtros prÃƒÂ³prios.
- [ ] Triagem deixa de ser planned view ou fica explicitamente limitada.
- [ ] Auditoria e permissÃƒÂµes protegem aÃƒÂ§ÃƒÂµes documentais.
- [ ] Soft delete substitui delete real.
- [ ] Dashboard mostra lacunas e contadores de governanÃƒÂ§a.
- [ ] Testes especÃƒÂ­ficos do Hub passam.

---

## 11. EntregÃƒÂ¡veis principais

### Sem R5

- `data/centralDocumentsManifest.ts` Ã¢â‚¬â€ novo.
- `services/centralPadroesDocumentHubService.ts` Ã¢â‚¬â€ novo.
- `utils` ou serviÃƒÂ§o de normalizadores documentais Ã¢â‚¬â€ novo.
- `components/MarkdownPreview.tsx` Ã¢â‚¬â€ novo.
- `pages/DocumentoDetalhePage.tsx` Ã¢â‚¬â€ novo.
- `pages/TriagemIngestaoPage.tsx` ou `pages/IngestionPage.tsx` Ã¢â‚¬â€ novo.
- `types/index.ts` Ã¢â‚¬â€ expandir tipos.
- `layout/CentralPadroesLayout.tsx` Ã¢â‚¬â€ navegaÃƒÂ§ÃƒÂ£o por detalhe.
- `pages/DocumentsPage.tsx` Ã¢â‚¬â€ refatorar lista e filtros.
- `pages/InternalDocsPage.tsx`, `ExternalDocsPage.tsx`, `ArchivePage.tsx` Ã¢â‚¬â€ filtros contextuais.
- `pages/SearchPage.tsx`, `ChatPietroPage.tsx` Ã¢â‚¬â€ abrir detalhe real.
- `services/centralPadroesIndexService.ts`, `centralPadroesSearchService.ts`, `centralPadroesChatPietroService.ts` Ã¢â‚¬â€ fonte unificada.
- `services/centralPadroesCrudService.ts` Ã¢â‚¬â€ mapDocument expandido e soft delete.
- `services/centralPadroesAuditService.ts`, `centralPadroesPermissionService.ts` Ã¢â‚¬â€ integraÃƒÂ§ÃƒÂ£o documental.
- Testes novos para Hub.

### Com R5

- Migration de schema para `central_padroes_documents`.
- PadronizaÃƒÂ§ÃƒÂ£o de storage buckets.
- RevisÃƒÂ£o/expansÃƒÂ£o da RPC `central_padroes_ingest_document`.
- PolÃƒÂ­ticas RLS/policies para aÃƒÂ§ÃƒÂµes documentais.
- Editor markdown persistente.
- IntegraÃƒÂ§ÃƒÂ£o opcional com `central_padroes_search_index`/pgvector.

---

## 12. ConclusÃƒÂ£o

O caminho seguro ÃƒÂ© nÃƒÂ£o iniciar pelo editor. A prioridade ÃƒÂ© primeiro criar uma fonte documental unificada que use o que jÃƒÂ¡ existe: CRUD, governance records, ÃƒÂ­ndice, busca, Chat Pietro, triagem, auditoria, permissÃƒÂµes e acervo `.md`. Depois, a UI deve abrir detalhe real, filtrar corretamente e expor lacunas sem falso salvamento. SÃƒÂ³ entÃƒÂ£o faz sentido executar o plano R5 para persistir conteÃƒÂºdo e metadados ricos no Supabase.

