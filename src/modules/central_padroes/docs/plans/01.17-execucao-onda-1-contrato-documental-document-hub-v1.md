# Plano 01.17 — Execução Onda 1 Contrato Documental — Document Hub V1

**Data:** 13-06-2026  
**Módulo:** `src/modules/central_padroes`  
**Origem:** Continuidade do arquivo `01.16-execucao-onda-0-baseline-document-hub-v1.md`.  
**Escopo:** Implementação inicial do contrato documental unificado sem R5.  
**Status:** concluído

---

## 1. Contexto Herdado

- Último arquivo lido: `01.16-execucao-onda-0-baseline-document-hub-v1.md`.
- Último checkpoint concluído: C1 — baseline.
- Checkpoint atual: C2 — contrato documental unificado.
- Próximo checkpoint esperado: C3 — navegação e detalhe documental.

---

## 2. Objetivo desta Etapa

Criar uma base documental enriquecida e não-R5, usando campos opcionais, manifest local e fachada única para documentos do Hub.

---

## 3. Arquivos Alterados

| Arquivo | O que mudou |
|---|---|
| `src/modules/central_padroes/types/index.ts` | Adicionados tipos `CentralDocumentStatus`, `CentralDocumentType`, `CentralDocumentSource`, `CentralCanonicalLevel`, `CentralDocumentContentAvailability` e campos opcionais ricos em `CentralDocument`. |
| `src/modules/central_padroes/utils/documentNormalizers.ts` | Criados normalizadores e helpers de enriquecimento/search text. |
| `src/modules/central_padroes/data/centralDocumentsManifest.ts` | Criado manifest documental inicial com planos recentes e documentos mestres. |
| `src/modules/central_padroes/services/centralPadroesDocumentHubService.ts` | Criada fachada única para listar, buscar e sumarizar lacunas documentais. |
| `src/modules/central_padroes/services/centralPadroesCrudService.ts` | Expandido mapper documental para aceitar campos ricos quando existirem, sem exigir migration. |
| `src/modules/central_padroes/index.ts` | Exportados manifest e DocumentHubService. |

---

## 4. Validação Executada

```text
npm run build
```

Resultado: build concluído com sucesso. Permanecem apenas warnings já existentes de chunk size/circular chunk/dynamic import, sem erro de compilação.

---

## 5. Critério de Pronto

- [x] Existe entidade documental enriquecida.
- [x] Existe fachada única para documentos do Hub.
- [x] Campos novos são opcionais e não exigem schema R5.
- [x] Manifest documental inicial existe.
- [x] Build passa.

---

## 6. Pendências Herdadas

- Integrar UI de documentos ao `centralPadroesDocumentHubService`.
- Criar detalhe documental read-only.
- Criar preview markdown.
- Ajustar layout para abrir documento específico.
- Atualizar busca e Pietro para abrir detalhe real.
- Corrigir soft delete em onda posterior.
- Não executar R5 sem checkpoint explícito.

---

## 7. Instrução para o Próximo Agente

Continuar pela Onda 2 criando o próximo arquivo sequencial `01.18-execucao-onda-2-detalhe-navegacao-document-hub-v1.md`. A próxima implementação deve criar detalhe documental e navegação real por documento usando a fachada `centralPadroesDocumentHubService`, sem editor persistente e sem migrations/RLS/RPC/storage remoto.
