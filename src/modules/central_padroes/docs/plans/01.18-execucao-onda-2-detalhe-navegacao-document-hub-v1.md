# Plano 01.18 — Execução Onda 2 Detalhe e Navegação — Document Hub V1

**Data:** 13-06-2026  
**Módulo:** `src/modules/central_padroes`  
**Origem:** Continuidade do arquivo `01.17-execucao-onda-1-contrato-documental-document-hub-v1.md`.  
**Escopo:** UI de leitura, detalhe documental e navegação real por documento, sem R5.  
**Status:** concluído

---

## 1. Contexto Herdado

- Último arquivo lido: `01.17-execucao-onda-1-contrato-documental-document-hub-v1.md`.
- Último checkpoint concluído: C2 — contrato documental unificado.
- Checkpoint atual: C3 — navegação e detalhe documental.
- Próximo checkpoint esperado: C4 — consistência entre listas, busca e Pietro.

---

## 2. Objetivo desta Etapa

Permitir abrir um documento específico em tela de detalhe real, exibir metadados enriquecidos, caminhos, lacunas e preview markdown read-only.

---

## 3. Arquivos Alterados

| Arquivo | O que mudou |
|---|---|
| `src/modules/central_padroes/components/MarkdownPreview.tsx` | Criado preview markdown simples e seguro, com fallback para conteúdo ausente. |
| `src/modules/central_padroes/pages/DocumentoDetalhePage.tsx` | Criada tela de detalhe documental read-only. |
| `src/modules/central_padroes/pages/DocumentsPage.tsx` | Lista passou a consumir `centralPadroesDocumentHubService`, aceitar filtros iniciais e abrir documento por callback. |
| `src/modules/central_padroes/layout/CentralPadroesLayout.tsx` | Adicionada view `documento-detalhe`, estado `selectedDocumentId` e callback `openDocument`. |
| `src/modules/central_padroes/pages/SearchPage.tsx` | Preparada para abrir documentos por callback quando o resultado for documental. |
| `src/modules/central_padroes/pages/ChatPietroPage.tsx` | Preparada para abrir fontes documentais por callback. |
| `src/modules/central_padroes/pages/InternalDocsPage.tsx` | Wrapper agora aplica filtro/contexto e repassa callback. |
| `src/modules/central_padroes/pages/ExternalDocsPage.tsx` | Wrapper agora aplica filtro/contexto e repassa callback. |
| `src/modules/central_padroes/pages/ArchivePage.tsx` | Wrapper agora aplica contexto de arquivo/legado e repassa callback. |

---

## 4. Validação Executada

```text
npm run build
```

Resultado: build concluído com sucesso. Permanecem apenas warnings existentes de chunk size/circular chunk/dynamic import.

---

## 5. Critério de Pronto

- [x] Documento pode ser aberto por ID em detalhe real.
- [x] Detalhe exibe metadados ricos e lacunas.
- [x] Preview markdown existe com fallback seguro.
- [x] Layout possui estado para documento selecionado.
- [x] Busca e Pietro já têm callback para abertura real.
- [x] Não foi criado editor persistente.
- [x] Nenhuma migration/RLS/RPC/storage remoto foi alterado.

---

## 6. Pendências Herdadas

- Fazer busca textual consumir o DocumentHubService como fonte documental.
- Fazer índice/Pietro consumir DocumentHubService.
- Melhorar colunas/filtros avançados da lista.
- Corrigir soft delete e auditoria documental em ondas posteriores.
- Criar página real de triagem.

---

## 7. Instrução para o Próximo Agente

Continuar pela Onda 3 criando o próximo arquivo sequencial `01.19-execucao-onda-3-busca-pietro-listas-document-hub-v1.md`. A próxima implementação deve unificar listas, busca, índice e Chat Pietro sobre `centralPadroesDocumentHubService`, mantendo tudo sem R5.
