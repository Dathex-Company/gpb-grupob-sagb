# Plano 01.22 Ã¢â‚¬â€ ExecuÃƒÂ§ÃƒÂ£o R5 PersistÃƒÂªncia Ã¢â‚¬â€ Document Hub V1

**Data:** 13-06-2026
**Hora:** 11:54 (UTC-3)
**MÃƒÂ³dulo:** `src/modules/central_padroes`
**Origem:** Continuidade do arquivo `01.21-autorizacao-r5-schema-rls-rpc-storage-document-hub-v1-13-06-2026.md`.  
**Escopo:** CriaÃƒÂ§ÃƒÂ£o local revisÃƒÂ¡vel de migrations R5 e upload service para Document Hub V1.  
**Status:** concluÃƒÂ­do localmente

---

## 1. Contexto Herdado

- ÃƒÅ¡ltimo arquivo lido: `01.21-autorizacao-r5-schema-rls-rpc-storage-document-hub-v1-13-06-2026.md`.
- ÃƒÅ¡ltimo checkpoint concluÃƒÂ­do: C6 Ã¢â‚¬â€ autorizaÃƒÂ§ÃƒÂ£o/timestamp R5.
- Checkpoint atual: C7 Ã¢â‚¬â€ persistÃƒÂªncia local revisÃƒÂ¡vel.
- PrÃƒÂ³ximo checkpoint esperado: C8 Ã¢â‚¬â€ editor markdown persistente.

---

## 2. Arquivos Criados

| Arquivo | Finalidade |
|---|---|
| `supabase/migrations/20260613000101_central_padroes_document_hub_v1_documents.sql` | Adiciona campos ricos e ÃƒÂ­ndices em `central_padroes_documents`. |
| `supabase/migrations/20260613000102_central_padroes_document_hub_v1_rls.sql` | Cria policies RLS action-based para documentos. |
| `supabase/migrations/20260613000103_central_padroes_document_hub_v1_ingest_rpc.sql` | Recria RPC `central_padroes_ingest_document` com contrato expandido. |

---

## 3. Arquivos Alterados

| Arquivo | O que mudou |
|---|---|
| `services/supabase.ts` | Exporta `supabaseConfig` e `getSupabaseAccessToken` para upload via Storage REST. |
| `src/modules/central_padroes/services/centralPadroesStorageService.ts` | Adiciona contrato expandido de ingestÃƒÂ£o e helpers de upload para `cp-documents` e `cp-evidence`. |

---

## 4. ValidaÃƒÂ§ÃƒÂ£o Executada

```text
npm run build
```

Resultado: build concluÃƒÂ­do com sucesso. NÃƒÂ£o houve aplicaÃƒÂ§ÃƒÂ£o remota das migrations nesta etapa.

---

## 5. CritÃƒÂ©rio de Pronto

- [x] Migration de schema rico criada.
- [x] Migration de RLS/policies criada.
- [x] Migration da RPC revisada criada.
- [x] Upload service usa buckets oficiais `cp-documents` e `cp-evidence`.
- [x] Path conventions implementadas no service.
- [x] Build passa.
- [x] Nenhum comando remoto de aplicaÃƒÂ§ÃƒÂ£o Supabase foi executado.

---

## 6. Riscos Remanescentes

- Policies RLS precisam ser testadas por perfil em Supabase real.
- RPC deve ser validada contra a estrutura exata da tabela `central_padroes_ingestion_queue` no ambiente remoto.
- Migrations locais ainda precisam ser aplicadas em ambiente controlado antes de confiar no editor persistente em produÃƒÂ§ÃƒÂ£o.

---

## 7. InstruÃƒÂ§ÃƒÂ£o para o PrÃƒÂ³ximo Agente

Continuar pela Onda 6 criando `01.23-execucao-editor-markdown-persistente-document-hub-v1-13-06-2026.md`. O editor deve salvar via `centralPadroesCrudService.updateDocument()` e opcionalmente enviar markdown para `cp-documents` usando `centralPadroesStorageService.uploadMarkdownDocument()`, com auditoria e reindexaÃƒÂ§ÃƒÂ£o.
