97.5 — Tarefa de Auditoria Técnica, Refatoração, Supabase e Edição no Módulo Central de Padrões — v1.0 — 07-06-2026

Cássio, tudo bem?

Execute esta tarefa com atenção máxima.

Esta tarefa é para analisar profundamente o módulo `central_padroes` do SagB após a subida do acervo `MD-central-de-padroes`.

O objetivo agora é verificar se o módulo está em condições reais de uso, principalmente para:

1. visualizar os Documentos Mestres;
2. alterar/editar documentos dentro do próprio SagB;
3. salvar alterações corretamente;
4. conferir integração com Supabase;
5. conferir tabelas, RPCs, buckets, RLS e serviços;
6. corrigir links, rotas, dados, páginas, imports e qualquer item quebrado;
7. refatorar pontos seguros que estiverem desalinhados;
8. preparar o módulo para teste visual e funcional pelo Rodrigues.

Não fazer commit.  
Não fazer push.  
Não fazer deploy.  
Não alterar nada fora de:

```text
Z:\00_sagb

Não marcar nenhum documento como canonico_oficial.

Não apagar dados.

Não fazer alteração destrutiva em banco, migration ou Supabase.

1. Caminhos principais
Projeto SagB
Z:\00_sagb
Módulo Central de Padrões
Z:\00_sagb\src\modules\central_padroes
Pasta de planos e relatórios desta tarefa
Z:\00_sagb\src\modules\central_padroes\plan
Acervo documental já usado como origem
Z:\MD-central-de-padroes

Se o caminho Z:\MD-central-de-padroes não existir, apenas registrar no relatório.
Nesta tarefa, o foco principal é o módulo dentro de Z:\00_sagb.

2. Primeiro passo obrigatório: criar o plano 98.5

Antes de alterar qualquer arquivo, criar dentro de:

Z:\00_sagb\src\modules\central_padroes\plan

o arquivo:

98.5-plano-auditoria-tecnica-refatoracao-supabase-edicao-central-padroes-v1.0-07-06-2026.md

O plano deve conter:

objetivo;
arquivos e pastas que serão analisados;
estado atual do módulo central_padroes;
páginas atuais encontradas;
rotas/views atuais encontradas;
dados atuais encontrados;
integração Supabase encontrada;
tabelas/RPCs/buckets esperados e encontrados;
fluxo atual de edição/salvamento, se existir;
pontos quebrados encontrados inicialmente;
refatorações seguras previstas;
riscos;
confirmação de que não haverá commit, push ou deploy.

Depois de criar o plano, executar a auditoria e as correções seguras.

3. Contexto da situação atual

Na etapa anterior, foram carregados no módulo:

12 Documentos Mestres v3.0 como em_curadoria;
documento-base 99 como GOV-PAD-002;
relatórios de curadoria 98.0, 98.1, 98.2 e 98.3;
55 subdocumentos previstos;
4 páginas novas:
DocumentosMestresPage.tsx;
DocumentoBasePage.tsx;
RelatoriosPage.tsx;
SubdocumentosPrevistosPage.tsx;
sidebar atualizada;
build validado.

Agora é preciso verificar se isso ficou tecnicamente consistente e se o módulo já está preparado para operar como Central de Padrões real.

4. Regra normativa central

Preservar esta regra:

MD-central-de-padroes = acervo documental auditável.
Supabase = fonte operacional canônica.
Módulo SagB = interface executora e consultável.
Fallback = contingência.

Nesta tarefa, verificar se o módulo está caminhando para essa regra.

Se hoje ainda estiver dependente de fallbackData.ts, registrar isso claramente.

Se já houver integração Supabase, conferir se está funcional.

Se houver divergência entre fallback e Supabase, registrar.

Não remover fallback nesta tarefa, a menos que esteja claramente quebrando a aplicação.

Fallback deve ser tratado como contingência, não como fonte canônica final.

5. Auditoria das páginas e rotas

Analisar todas as páginas do módulo central_padroes.

Verificar:

se as páginas importam corretamente;
se não há imports quebrados;
se a sidebar aponta para views válidas;
se CentralPadroesLayout.tsx reconhece todas as views;
se não há links mortos;
se não há view sem componente;
se não há componente sem rota;
se não há nomes divergentes entre sidebar, layout e páginas;
se as 4 páginas novas aparecem corretamente;
se as páginas existentes não foram quebradas.

Páginas novas que precisam ser conferidas:

DocumentosMestresPage.tsx
DocumentoBasePage.tsx
RelatoriosPage.tsx
SubdocumentosPrevistosPage.tsx

Arquivos prováveis a conferir:

Z:\00_sagb\src\modules\central_padroes\pages
Z:\00_sagb\src\modules\central_padroes\layout
Z:\00_sagb\src\modules\central_padroes\data\sidebarConfig.ts
Z:\00_sagb\src\modules\central_padroes\data\fallbackData.ts
6. Auditoria da capacidade de edição dentro do SagB

Verificar se o módulo já permite alterar documentos dentro do próprio SagB.

Analisar:

existe botão de editar?
existe formulário de edição?
existe viewer Markdown?
existe editor Markdown?
existe fluxo de salvar?
salvar altera fallback, Supabase ou apenas estado local?
existe controle de status?
existe histórico de alteração?
existe validação de permissão?
existe confirmação antes de salvar?
existe registro de auditoria?
existe tratamento de erro?
existe indicação de que documento está em_curadoria?
existe bloqueio para impedir marcar canonico_oficial indevidamente?

Se a edição existir, testar tecnicamente o fluxo.

Se a edição não existir, registrar no relatório e propor implementação.

Se for simples e seguro implementar uma primeira camada de edição local/controlada, pode implementar, desde que:

não marque nada como canonico_oficial;
não publique nada;
não apague dados;
não dependa de gambiarra;
registre a alteração;
mantenha status em_curadoria;
deixe claro se salva em Supabase ou fallback.

Prioridade da edição:

1. editar metadados do documento;
2. editar status permitido, sem canonicidade oficial;
3. editar resumo, palavras-chave e pendências;
4. visualizar conteúdo Markdown;
5. preparar futura edição do conteúdo completo.

Não criar editor complexo se isso exigir arquitetura grande.
Se for grande, registrar como pendência.

7. Auditoria do Supabase

Verificar profundamente se o módulo já está conectado ao Supabase.

Analisar:

onde está o client Supabase;
quais services usam Supabase;
quais tabelas são consultadas;
quais tabelas são esperadas;
quais RPCs existem no código;
quais RPCs são chamadas;
quais buckets são usados;
se existe RLS considerada;
se existe tratamento de erro;
se existe fallback quando Supabase falha;
se existe mensagem de contingência;
se existe distinção clara entre fonte canônica e fallback.

Arquivos prováveis a conferir:

Z:\00_sagb\src\modules\central_padroes
Z:\00_sagb\services
Z:\00_sagb\src\services
Z:\00_sagb\supabase
Z:\00_sagb\src\modules\central_padroes\services
Z:\00_sagb\src\modules\central_padroes\data

Se houver migrations, schemas ou SQLs, analisar também.

8. Tabelas Supabase esperadas

Verificar se existem ou se estão previstas tabelas compatíveis com a Central de Padrões.

Tabelas esperadas, nomes sugeridos:

cp_domains
cp_documents
cp_standards
cp_planned_subdocuments
cp_reports
cp_evidence
cp_dependencies
cp_decisions
cp_gaps
cp_audit_logs
cp_chat_logs
cp_status_history
cp_permissions

Não criar tabelas diretamente em produção sem confirmação.

Se a arquitetura do projeto usa migrations, pode criar migration segura apenas se fizer sentido e estiver dentro do padrão do projeto.

Não fazer migration destrutiva.

Não apagar tabela.

Não dropar coluna.

Não alterar dado existente sem backup/registro.

Se as tabelas já existem com outros nomes, mapear equivalência.

Relatório deve dizer:

tabela esperada
tabela encontrada
status
ação recomendada
9. RPCs esperadas

Verificar se existem chamadas ou SQLs para RPCs esperadas.

RPCs críticas esperadas:

cp_search_standards()
cp_get_dashboard_metrics()
cp_log_chat_pietro()
cp_approve_standard()
cp_reject_standard()
cp_publish_standard()
cp_reconcile_fallback()

Nesta tarefa, não é obrigatório implementar todas, mas é obrigatório verificar:

se existem;
se o front chama;
se há fallback direto em tabela;
se ações críticas estão protegidas;
se aprovação/rejeição/publicação não são feitas direto por update simples.

Regra:

Aprovação, rejeição e publicação não devem depender de update direto no front.

Se ainda estiver assim, registrar como pendência crítica.

10. Buckets esperados

Verificar se o módulo usa ou espera buckets reais:

cp-documents
cp-evidence

Conferir:

nomes usados no código;
nomes divergentes;
upload/download;
links quebrados;
storage service;
tratamento de erro;
permissões esperadas.

Não apagar arquivos de bucket.

Não mover arquivos de bucket.

Se os buckets não existirem, registrar.

Se houver migration/config para buckets, registrar ou propor.

11. Status normativos permitidos

Verificar se o módulo reconhece e trata corretamente os status:

bruto
rascunho
em_revisao
em_curadoria
homologado
canonico_operacional
canonico_oficial
publicado
obsoleto
arquivado
bloqueado
precisa_validacao
previsto
registro

Checar:

enum/types;
filtros;
badges;
cores;
status dos Documentos Mestres;
status dos subdocumentos previstos;
status dos relatórios;
se algum documento foi marcado indevidamente como canonico_oficial.

Os 12 Documentos Mestres devem permanecer:

em_curadoria

Subdocumentos previstos:

previsto

Relatórios:

registro
12. Tipos normativos

Verificar se o módulo reconhece:

princípio
política
regra
padrão
protocolo
processo
procedimento
checklist
matriz
registro/evidência
decisão
risco
lacuna
documento_mestre
relatorio
padrão-base documental

Se tipos estiverem incompletos, ajustar de forma segura.

Não quebrar tipos existentes.

13. Auditoria dos dados carregados

Conferir os dados carregados em fallbackData.ts ou equivalente.

Verificar:

12 Documentos Mestres existem;
códigos DM-00-GOV a DM-11-NEG existem;
documento 99 existe como GOV-PAD-002;
relatórios 98.0 a 98.3 existem;
55 subdocumentos previstos existem;
responsáveis estão corretos;
status estão corretos;
versões estão corretas;
domínios estão corretos;
links/caminhos estão corretos;
filtros funcionam com esses dados;
não há duplicidade óbvia.

Atenção: Documentos Mestres não devem ser tratados como padrões atômicos finais.
Se estiverem em standards, registrar como decisão técnica provisória e propor separação futura.

Separação futura desejada:

Documentos Mestres = documents / master_documents
Padrões atômicos = standards
Subdocumentos previstos = planned_subdocuments
Relatórios = evidence / records
14. Refatoração segura

Pode refatorar de forma segura se encontrar:

import quebrado;
link quebrado;
view sem rota;
sidebar apontando errado;
componente duplicado simples;
tipo TypeScript incorreto;
status não reconhecido;
filtro quebrado;
build quebrado;
dado duplicado óbvio;
função morta claramente não usada;
nomenclatura divergente que cause erro.

Não fazer refatoração grande de arquitetura sem registrar e justificar.

Não trocar toda a arquitetura do módulo.

Não remover fallback.

Não trocar Supabase por fallback.

Não fazer alteração destrutiva.

15. Auditoria do Chat Pietro

Se já existir Chat Pietro no módulo, verificar:

se ele usa fonte real;
se mostra fontes utilizadas;
se mostra status do documento;
se avisa quando documento não é canônico;
se registra log;
se não inventa padrão;
se usa Supabase, fallback ou mock.

Regra obrigatória:

Se a fonte estiver em_curadoria, o Chat Pietro deve deixar claro que ainda não é canônico oficial.

Se o Chat Pietro ainda não existir, registrar como pendência.

Não implementar Chat Pietro completo nesta tarefa se for grande demais.

16. Auditoria de permissões

Verificar se o módulo contempla ou prepara papéis:

leitor
editor
curador
aprovador
administrador
agente_autorizado
auditor

Verificar se existe regra para:

leitor não edita
editor não publica
curador revisa
aprovador aprova/rejeita
administrador administra
agente autorizado não aprova output próprio
auditor vê logs

Se não existir, registrar como pendência.

Se for simples ajustar labels/tipos, pode ajustar.

Não implementar sistema inteiro de permissões nesta tarefa se for grande.

17. Auditoria visual técnica básica

Sem depender do Rodrigues testar visualmente, fazer uma verificação técnica básica:

página carrega sem erro;
tabela renderiza;
filtros não quebram;
links internos funcionam;
sidebar exibe itens;
páginas novas respondem;
badges de status aparecem;
contadores não quebram;
dados vazios têm estado vazio;
erro tem mensagem clara.

Se não for possível testar em navegador, registrar.

18. Build e validação

Ao final, executar:

npm run build

Ou comando padrão do projeto.

Registrar:

comando executado;
resultado;
tempo;
erros;
warnings;
se warnings são pré-existentes ou novos.

Se o build falhar, corrigir se for dentro do escopo.

Se falhar por ambiente/filesystem, registrar.

19. Relatório final obrigatório

Criar dentro de:

Z:\00_sagb\src\modules\central_padroes\plan

o arquivo:

98.5-relatorio-auditoria-tecnica-refatoracao-supabase-edicao-central-padroes-v1.0-07-06-2026.md

O relatório deve conter:

objetivo;
arquivos analisados;
arquivos alterados;
páginas analisadas;
páginas corrigidas;
situação da edição de documentos dentro do SagB;
situação do Supabase;
tabelas encontradas;
tabelas esperadas;
RPCs encontradas;
RPCs esperadas;
buckets encontrados;
buckets esperados;
status normativos conferidos;
tipos normativos conferidos;
dados do acervo conferidos;
problemas encontrados;
correções aplicadas;
refatorações realizadas;
pendências críticas;
pendências não críticas;
recomendações para próxima etapa;
resultado do build;
confirmação de que nada foi marcado como canonico_oficial;
confirmação de que não houve alteração destrutiva;
confirmação de que nada fora do escopo foi alterado;
confirmação de que não houve commit, push ou deploy.
20. Resposta final esperada no chat

Ao terminar, responder:

Auditoria técnica, refatoração, Supabase e edição do módulo Central de Padrões concluída.

1. Plano 98.5 criado:
2. Arquivos analisados:
3. Arquivos alterados:
4. Situação da edição dentro do SagB:
5. Situação do Supabase:
6. Tabelas/RPCs/buckets conferidos:
7. Problemas encontrados:
8. Correções/refatorações aplicadas:
9. Pendências críticas:
10. Pendências não críticas:
11. Build executado:
12. Relatório 98.5 criado:
13. Confirmação de que nada foi marcado como canonico_oficial.
14. Confirmação de que não houve alteração destrutiva.
15. Confirmação de que nada fora do escopo foi alterado.
16. Confirmação de que não houve commit, push ou deploy.

Fechamento esperado:

Módulo Central de Padrões auditado tecnicamente, refatorado onde era seguro, com situação de edição e Supabase mapeada para o próximo te