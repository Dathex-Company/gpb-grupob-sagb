# Plano 01.20 — Execução Onda 4 Governança Operacional — Document Hub V1

**Data:** 13-06-2026  
**Módulo:** `src/modules/central_padroes`  
**Origem:** Continuidade do arquivo `01.19-execucao-onda-3-busca-pietro-listas-document-hub-v1.md`.  
**Escopo:** Governança operacional sem R5: permissões documentais, soft delete, auditoria, dashboard de lacunas, triagem real e reconciliação não destrutiva.  
**Status:** concluído

---

## 1. Contexto Herdado

- Último arquivo lido: `01.19-execucao-onda-3-busca-pietro-listas-document-hub-v1.md`.
- Último checkpoint concluído: C4 — listas, busca, índice e Pietro usando base compatível.
- Checkpoint atual: C5 — governança operacional.
- Próximo checkpoint esperado: C6 — autorização explícita para R5.

---

## 2. Objetivo desta Etapa

Conectar o Document Hub às capacidades operacionais de governança já existentes, sem alterar schema remoto, RLS, RPC ou storage.

---

## 3. Arquivos Alterados

| Arquivo | O que mudou |
|---|---|
| `src/modules/central_padroes/types/index.ts` | Adicionadas ações documentais em `CentralAction` e matriz de permissões para criar, editar, publicar, arquivar, restaurar e enviar documentos à curadoria. |
| `src/modules/central_padroes/services/centralPadroesPermissionService.ts` | Adicionados helpers documentais: `canCreateDocument`, `canEditDocument`, `canPublishDocument`, `canArchiveDocument`, `canRestoreDocument`, `canSendDocumentToCuradoria`. |
| `src/modules/central_padroes/services/centralPadroesCrudService.ts` | Documentos agora registram auditoria em create/update/delete; `deleteDocument()` foi trocado de DELETE real para soft delete via PATCH com `deleted_at`, `deleted_by` e status `arquivado`. |
| `src/modules/central_padroes/pages/DashboardPage.tsx` | Dashboard passou a exibir lacunas do Document Hub: total, incompletos, sem owner, sem tags, sem resumo e sem conteúdo. |
| `src/modules/central_padroes/pages/TriagemIngestaoPage.tsx` | Criada página real de triagem e ingestão com listar, aceitar, ignorar e abrir documentos. |
| `src/modules/central_padroes/layout/CentralPadroesLayout.tsx` | View `ingestion` deixou de ser planned view e passou a renderizar `TriagemIngestaoPage`. |
| `src/modules/central_padroes/services/centralPadroesReconciliationService.ts` | Adicionado relatório `generateDocumentHubMarkdownReport()` para lacunas e distribuição do Hub sem merge automático. |

---

## 4. Validação Executada

```text
npm run build
```

Resultado: build concluído com sucesso. Permanecem apenas warnings existentes de bundle/chunks/dynamic import, sem erro de compilação.

---

## 5. Critério de Pronto

- [x] Permissões documentais existem no frontend/service.
- [x] Soft delete substitui DELETE real em documentos.
- [x] Auditoria é chamada em create/update/delete documental.
- [x] Dashboard mostra lacunas documentais.
- [x] Triagem/Ingestion deixou de ser planned view e virou página operacional.
- [x] Reconciliação Document Hub gera relatório não destrutivo.
- [x] Build passa.
- [x] Nenhuma migration/RLS/RPC/storage remoto foi alterado.

---

## 6. Pendências Herdadas

- Executar R5: schema rico, índices, RLS action-based, RPC revisada e upload real.
- Implementar editor markdown persistente após R5 validado.
- Testar RLS por perfil depois das migrations.
- Criar relatório final de fechamento após R5/editor/testes.

---

## 7. Checkpoint R5

A próxima etapa é `C6 — Autorização R5`. A partir daqui, qualquer avanço envolve pelo menos uma destas alterações:

- criação de migrations em `supabase/migrations/`;
- alteração de schema da tabela `central_padroes_documents`;
- criação/ajuste de índices;
- refinamento de RLS/policies;
- revisão da RPC `central_padroes_ingest_document`;
- implementação de upload real em buckets `cp-documents` e `cp-evidence`.

Essas ações exigem autorização explícita antes de continuar.

---

## 8. Instrução para o Próximo Agente

Antes de executar R5, confirmar autorização explícita do usuário. Se autorizado, criar o próximo arquivo sequencial `01.21-autorizacao-r5-schema-rls-rpc-storage-document-hub-v1.md` registrando a autorização, timestamp livre de migration, escopo exato e plano de rollback. Depois executar migrations/RLS/RPC/storage em blocos pequenos com build/teste entre eles.
