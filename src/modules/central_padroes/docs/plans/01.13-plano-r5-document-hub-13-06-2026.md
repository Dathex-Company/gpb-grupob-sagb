# Plano 01.13 Ã¢â‚¬â€ R5 Document Hub V1 Ã¢â‚¬â€ Supabase Schema, Storage, RPC e PersistÃƒÂªncia

Data: 2026-06-13
Hora: 11:39 (UTC-3)
MÃƒÂ³dulo: `src/modules/central_padroes`
Origem: itens R5 identificados no plano consolidado `01.12-plano-execucao-consolidado-document-hub-v1-13-06-2026.md`.  
RestriÃƒÂ§ÃƒÂ£o: este documento ÃƒÂ© apenas plano. Nenhuma migration, policy, RPC, storage ou cÃƒÂ³digo-fonte foi alterado nesta etapa.

---

## 1. Objetivo

Isolar tudo que exige alteraÃƒÂ§ÃƒÂ£o de Supabase remoto, schema, storage, RLS/policies, RPC ou pipeline semÃƒÂ¢ntico para que o Document Hub V1 possa evoluir sem misturar tarefas seguras de frontend/read-only com operaÃƒÂ§ÃƒÂµes R5.

---

## 2. Itens R5 identificados

| ÃƒÂrea | Item | Motivo R5 |
|---|---|---|
| Schema | Expandir `central_padroes_documents` | PersistÃƒÂªncia rica de documentos vivos. |
| Schema | Criar ÃƒÂ­ndices para campos novos | Performance de filtros e busca. |
| RLS/policies | Ajustar permissÃƒÂµes documentais | AÃƒÂ§ÃƒÂµes por perfil precisam ser coerentes no banco. |
| Service/RLS | Soft delete obrigatÃƒÂ³rio | Evitar DELETE real e preservar histÃƒÂ³rico. |
| Storage | Padronizar buckets oficiais | HÃƒÂ¡ divergÃƒÂªncia entre service, module-doc e migrations. |
| RPC | Revisar `central_padroes_ingest_document` | Contrato atual precisa atender Hub, triagem, storage e auditoria. |
| Storage/RPC | Upload real de documento/evidÃƒÂªncia | Hoje o service nÃƒÂ£o faz upload em bucket. |
| Search | Ativar `central_padroes_search_index`/pgvector no fluxo | Schema existe, mas frontend ainda usa busca textual. |
| Editor | Salvar conteÃƒÂºdo markdown | Requer `content` ou storage path persistente. |

---

## 3. DiagnÃƒÂ³stico R5 atual

### 3.1 Tabela `central_padroes_documents`

Estado atual identificado nas migrations:

- criada com campos mÃƒÂ­nimos: `id`, `title`, `source_path`, `status`, `category`, `area_id`, `destination_type`, `created_at`, `updated_at`;
- migration V3 adicionou `deleted_at` e `deleted_by`;
- RLS atual permite SELECT com `deleted_at is null`, INSERT por perfis editor+ e UPDATE por curador/aprovador/admin;
- policy chamada `cp_documents_delete` ÃƒÂ©, na prÃƒÂ¡tica, policy de UPDATE para soft delete;
- frontend ainda executa DELETE real em `centralPadroesCrudService.deleteDocument()`.

Campos necessÃƒÂ¡rios para Document Hub persistente:

- `slug`;
- `type`;
- `risk_level`;
- `owner`;
- `tags`;
- `summary`;
- `content`;
- `path_absolute`;
- `path_relative`;
- `source`;
- `module`;
- `division`;
- `canonical_level`;
- `created_by`;
- `updated_by`.

### 3.2 Storage

DivergÃƒÂªncias encontradas:

| Fonte | Buckets citados |
|---|---|
| `module-doc.ts` | `canonical-documents`, `evidence-files` |
| `centralPadroesStorageService.ts` | `central-padroes-documents`, `central-padroes-evidence`, `central-padroes-templates` |
| migration V5 | `cp-documents`, `cp-evidence` |

DecisÃƒÂ£o recomendada: padronizar em `cp-documents` e `cp-evidence`, pois jÃƒÂ¡ existem na migration V5 e possuem policies em `storage.objects`.

### 3.3 RPC de ingestÃƒÂ£o

RPC encontrada: `central_padroes_ingest_document`.

Contrato atual no frontend:

- endpoint REST: `rpc/central_padroes_ingest_document`;
- parÃƒÂ¢metros enviados: `p_title`, `p_source_path`, `p_raw_content`, `p_source_kind`;
- retorno tratado como string.

Lacunas:

- retorno deveria ser estruturado com `queue_id`, `document_id`, `status`, `warnings`;
- nÃƒÂ£o hÃƒÂ¡ integraÃƒÂ§ÃƒÂ£o clara com storage path;
- nÃƒÂ£o registra auditoria detalhada da ingestÃƒÂ£o;
- nÃƒÂ£o valida bucket;
- nÃƒÂ£o classifica origem no vocabulÃƒÂ¡rio do Hub;
- nÃƒÂ£o preenche campos ricos da futura tabela `central_padroes_documents`.

### 3.4 Search index e pgvector

`central_padroes_search_index` existe com `embedding` e `search_vector`, mas o frontend mantÃƒÂ©m busca textual ampliada.

R5/fase futura necessÃƒÂ¡ria para:

- definir pipeline de reindexaÃƒÂ§ÃƒÂ£o;
- criar ou revisar RPCs de upsert/rebuild;
- gerar embeddings;
- controlar permissÃƒÂµes por `allowed_roles`;
- sincronizar DocumentHubService, SearchService, IndexService e Chat Pietro.

---

## 4. Migration proposta para schema documental

Nome sugerido:

`supabase/migrations/20260613000101_central_padroes_document_hub_v1_documents.sql`

ConteÃƒÂºdo conceitual:

```sql
alter table public.central_padroes_documents
  add column if not exists slug text,
  add column if not exists type text not null default 'documento',
  add column if not exists risk_level text not null default 'baixo',
  add column if not exists owner text,
  add column if not exists tags text[] not null default '{}',
  add column if not exists summary text,
  add column if not exists content text,
  add column if not exists path_absolute text,
  add column if not exists path_relative text,
  add column if not exists source text not null default 'manual',
  add column if not exists module text,
  add column if not exists division text,
  add column if not exists canonical_level text,
  add column if not exists created_by uuid references auth.users(id),
  add column if not exists updated_by uuid references auth.users(id);

create index if not exists idx_cp_documents_slug on public.central_padroes_documents(slug);
create index if not exists idx_cp_documents_type on public.central_padroes_documents(type);
create index if not exists idx_cp_documents_risk on public.central_padroes_documents(risk_level);
create index if not exists idx_cp_documents_owner on public.central_padroes_documents(owner);
create index if not exists idx_cp_documents_source on public.central_padroes_documents(source);
create index if not exists idx_cp_documents_canonical_level on public.central_padroes_documents(canonical_level);
create index if not exists idx_cp_documents_tags on public.central_padroes_documents using gin(tags);
```

ObservaÃƒÂ§ÃƒÂµes:

- avaliar `check` constraints para `risk_level`, `source`, `type` e `canonical_level` somente depois de confirmar vocabulÃƒÂ¡rio final;
- se houver dados legados com status divergentes, usar normalizaÃƒÂ§ÃƒÂ£o no frontend antes de constraints fortes;
- `slug` pode ser unique posteriormente, mas inicialmente pode haver colisÃƒÂµes entre `.md`, Supabase e governance.

---

## 5. RLS/policies propostas

### 5.1 AÃƒÂ§ÃƒÂµes documentais esperadas

- visualizar documento;
- criar documento;
- editar metadados documentais;
- editar conteÃƒÂºdo documental;
- enviar para curadoria;
- publicar/canonizar documento;
- arquivar documento;
- restaurar documento;
- anexar evidÃƒÂªncia;
- reindexar documento.

### 5.2 Regras recomendadas

| Perfil | Regras de documento |
|---|---|
| leitor | SELECT de documentos nÃƒÂ£o deletados e permitidos. |
| editor | INSERT e ediÃƒÂ§ÃƒÂ£o de rascunhos prÃƒÂ³prios, sem publicar. |
| curador | ediÃƒÂ§ÃƒÂ£o de metadados, triagem, curadoria, arquivamento nÃƒÂ£o crÃƒÂ­tico. |
| aprovador | aprovar/canonizar/publicar conforme gates. |
| administrador | todas as aÃƒÂ§ÃƒÂµes, incluindo restore e ajustes de schema operacional. |
| auditor | SELECT e logs, sem alteraÃƒÂ§ÃƒÂ£o. |
| agente_autorizado | leitura conforme allowed_roles e registro de consulta, sem ediÃƒÂ§ÃƒÂ£o. |

### 5.3 FunÃƒÂ§ÃƒÂµes auxiliares recomendadas

- `cp_can_edit_document(document_id uuid)`;
- `cp_can_edit_document_content(document_id uuid)`;
- `cp_can_publish_document(document_id uuid)`;
- `cp_can_archive_document(document_id uuid)`;
- `cp_can_restore_document(document_id uuid)`.

---

## 6. Storage e buckets

### 6.1 DecisÃƒÂ£o de nomes

Padronizar:

- `cp-documents` para documentos, markdown, PDF, DOCX e texto;
- `cp-evidence` para evidÃƒÂªncias, prints, PDFs e anexos de validaÃƒÂ§ÃƒÂ£o.

### 6.2 Ajustes necessÃƒÂ¡rios

- atualizar `module-doc.ts` para refletir buckets reais;
- atualizar `centralPadroesStorageService.ts` para usar `cp-documents` e `cp-evidence`;
- remover ou justificar `central-padroes-templates`, ou criar bucket correspondente em migration prÃƒÂ³pria;
- definir path convention:
  - `documents/{document_id}/{slug}.md`;
  - `documents/{document_id}/attachments/{filename}`;
  - `evidence/{entity_type}/{entity_id}/{timestamp}-{filename}`.

### 6.3 Policies de storage

Validar se policies existentes cobrem:

- select autenticado;
- insert por editor+;
- update por curador+;
- remoÃƒÂ§ÃƒÂ£o lÃƒÂ³gica ou restriÃƒÂ§ÃƒÂ£o de delete;
- separaÃƒÂ§ÃƒÂ£o de evidÃƒÂªncia crÃƒÂ­tica.

---

## 7. RPC de ingestÃƒÂ£o revisada

Nome recomendado:

- manter `central_padroes_ingest_document` com contrato expandido; ou
- criar `cp_document_hub_ingest_document` e manter a antiga como compatibilidade.

ParÃƒÂ¢metros sugeridos:

```sql
p_title text,
p_source_path text default null,
p_raw_content text default null,
p_source_kind text default 'manual',
p_storage_bucket text default null,
p_storage_path text default null,
p_owner text default null,
p_tags text[] default '{}',
p_risk_level text default 'baixo',
p_module text default null,
p_division text default null,
p_create_document boolean default false
```

Retorno sugerido:

```json
{
  "queue_id": "uuid",
  "document_id": "uuid|null",
  "status": "queued|created|triage|blocked",
  "warnings": [],
  "source": "manual|md_indexado|upload|external"
}
```

Comportamento esperado:

- registrar item na fila de ingestÃƒÂ£o;
- opcionalmente criar documento em `central_padroes_documents` quando autorizado;
- preencher metadados ricos quando disponÃƒÂ­veis;
- registrar audit log;
- retornar estrutura previsÃƒÂ­vel;
- nunca prometer salvar arquivo local em `Z:\`.

---

## 8. Editor persistente e conteÃƒÂºdo markdown

DependÃƒÂªncias antes de implementar `DocumentoEditorPage` com salvamento real:

- schema documental expandido aplicado;
- RLS/policies revisadas;
- decisÃƒÂ£o entre `content` em tabela e/ou arquivo em storage;
- service frontend usando campos novos;
- audit log em salvar conteÃƒÂºdo;
- reindex apÃƒÂ³s salvar;
- fallback de erro claro.

Regras:

- sem R5 aplicado, editor pode existir apenas em modo preview/readonly ou metadados bÃƒÂ¡sicos;
- botÃƒÂ£o Ã¢â‚¬Å“Salvar conteÃƒÂºdoÃ¢â‚¬Â deve ficar oculto/desabilitado se persistÃƒÂªncia real nÃƒÂ£o estiver confirmada;
- qualquer salvamento de conteÃƒÂºdo deve registrar `UPDATE` ou `CANONICAL_EDIT` conforme canonicidade.

---

## 9. Search index e pgvector

Fase R5 opcional/posterior ao Hub V1 funcional:

- definir fonte de embeddings;
- criar job/RPC de rebuild;
- popular `central_padroes_search_index` a partir do DocumentHubService ou equivalente server-side;
- sincronizar `allowed_roles`;
- integrar SearchService e Chat Pietro ao index remoto com fallback textual;
- manter auditoria de reindexaÃƒÂ§ÃƒÂ£o.

RPCs possÃƒÂ­veis:

- `cp_rebuild_document_hub_search_index()`;
- `cp_upsert_document_hub_search_entry(entity_type, entity_id)`;
- `cp_search_document_hub(query, filters jsonb)`.

---

## 10. Ordem R5 recomendada

1. Confirmar vocabulÃƒÂ¡rio final de `type`, `status`, `risk_level`, `source` e `canonical_level`.
2. Aplicar migration de campos ricos em `central_padroes_documents`.
3. Aplicar ÃƒÂ­ndices.
4. Revisar RLS/policies documentais.
5. Padronizar buckets para `cp-documents` e `cp-evidence`.
6. Atualizar ou criar RPC de ingestÃƒÂ£o com retorno estruturado.
7. Atualizar service frontend para buckets/RPC/schema novos.
8. Trocar delete real por soft delete no frontend.
9. Implementar salvamento real de conteÃƒÂºdo markdown.
10. Integrar audit log e reindex.
11. Validar com testes e QA autenticado.
12. SÃƒÂ³ entÃƒÂ£o liberar `DocumentoEditorPage` persistente.

---

## 11. CritÃƒÂ©rios de pronto R5

- [ ] `central_padroes_documents` possui campos ricos e ÃƒÂ­ndices aplicados.
- [ ] RLS/policies impedem aÃƒÂ§ÃƒÂµes indevidas por perfil.
- [ ] Buckets oficiais estÃƒÂ£o padronizados em schema, service e documentaÃƒÂ§ÃƒÂ£o.
- [ ] RPC de ingestÃƒÂ£o retorna estrutura previsÃƒÂ­vel e registra auditoria.
- [ ] Upload real de documentos/evidÃƒÂªncias funciona ou estÃƒÂ¡ explicitamente bloqueado.
- [ ] Soft delete substitui DELETE real.
- [ ] Editor salva conteÃƒÂºdo markdown de forma persistente.
- [ ] Salvamento registra audit log.
- [ ] Busca/reindex refletem conteÃƒÂºdo salvo.
- [ ] Rollback/mitigaÃƒÂ§ÃƒÂ£o estÃƒÂ¡ documentado antes de aplicar em remoto.

---

## 12. Riscos especÃƒÂ­ficos R5

| Risco | Impacto | MitigaÃƒÂ§ÃƒÂ£o |
|---|---|---|
| Migration em remoto sem backup | Perda/instabilidade de dados. | Snapshot/backup e execuÃƒÂ§ÃƒÂ£o controlada. |
| Constraints fortes antes de normalizar dados | Falhas em registros legados. | Adicionar constraints apÃƒÂ³s saneamento. |
| Buckets divergentes persistirem | Upload falha em produÃƒÂ§ÃƒÂ£o. | Padronizar todos os contratos em uma etapa. |
| RPC antiga quebrar UI existente | RegressÃƒÂ£o em `DocumentsPage`. | Manter compatibilidade ou versionar RPC nova. |
| RLS bloquear editor legÃƒÂ­timo | UsuÃƒÂ¡rios nÃƒÂ£o conseguem operar. | Testes por perfil antes de liberar. |
| RLS permissiva demais | AlteraÃƒÂ§ÃƒÂ£o indevida de documentos. | Policies por aÃƒÂ§ÃƒÂ£o e audit log obrigatÃƒÂ³rio. |
| ConteÃƒÂºdo em tabela crescer demais | Performance ruim. | Avaliar storage para arquivos grandes e `content` para markdown textual curto. |
| pgvector virar promessa nÃƒÂ£o entregue | ConfusÃƒÂ£o de produto. | Manter busca textual como modo declarado atÃƒÂ© pipeline pronto. |

---

## 13. ConclusÃƒÂ£o

O R5 do Document Hub deve ser tratado como trilha prÃƒÂ³pria. A V1 sem R5 pode entregar navegaÃƒÂ§ÃƒÂ£o, detalhe, busca, Chat Pietro consistente, triagem, auditoria, permissÃƒÂµes e lacunas visÃƒÂ­veis. O R5 deve entrar apenas quando houver autorizaÃƒÂ§ÃƒÂ£o para alterar Supabase, padronizar storage/RPC e persistir conteÃƒÂºdo de forma confiÃƒÂ¡vel.

