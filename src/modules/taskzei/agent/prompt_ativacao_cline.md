# prompt_ativacao_cline — taskzei  ## regra canônica obrigatória Antes de qualquer ação, este agente deve ler e obedecer integralmente: 1. `docs/governanca_sagb/padrao_unificado_governanca.md` 2. `docs/governanca_sagb/protocolo_log_continuo_agentes.md` 3. `docs/governanca_sagb/falas_user.md`  ## ordem de leitura do agente 1. `agent/prompt_ativacao_cline.md` 2. `agent/persona.md` 3. `agent/session_log.md` 4. `agent/falas_user.md`  ## estrutura obrigatória da pasta `agent` A pasta deste agente deve conter apenas os 4 arquivos canônicos: - `persona.md` - `session_log.md` - `falas_user.md` - `prompt_ativacao_cline.md`  É proibido recriar `owner.md`, `history-chat.md`, `history_chat.md`, `session-log.md`, `prompt-ativacao-cline.md` ou qualquer duplicata que faça a mesma função dos arquivos canônicos.  ## auto-log duplo obrigatório Antes de responder ao usuário, o agente deve registrar: 1. conversa completa e literal em `agent/session_log.md`; 2. fala do usuário, literal e isolada, em `agent/falas_user.md`. **Confirmação Visual:** Após registrar as interações nos arquivos de log, é estritamente obrigatório que você finalize a sua resposta no chat para o usuário contendo exatamente a tag: `[ 📝 Auto-log: OK ]`  ## literalidade Não resumir, corrigir, reescrever ou interpretar a fala do usuário nos logs. Registrar exatamente como foi dito/escrito.  ## calibração — Central de Documentos Inteligentes (ET D08-D12) Este módulo possui uma Central de Documentos Inteligentes integrada. Regras de operação: 1. **Árvore de documentos**: nós hierárquicos (pastas/documentos) com soft delete via `deleted_at`. Usar `buildDocTree()` para transformar flat list em árvore. 2. **Editor de blocos**: o conteúdo é salvo como blocos tipados em `taskzei_doc_contents`. Blocos: paragraph, heading, bulletList, orderedList, checkList, blockquote, codeBlock, divider. Auto-save com debounce de 1000ms. 3. **Links bidirecionais**: entidades se vinculam via `taskzei_entity_links` com metadata JSONB. Nunca usar campos avulsos para linkage. 4. **Exportação**: suporte a Markdown (.md) e HTML (.html) via `ExportActions.tsx`. 5. **Integração com tarefas**: o TaskDrawer exibe "Documentos de Apoio" com busca e vinculação. Metadata obrigatório: `{"intent": "execution_support", "linked_from": "task_drawer", "timestamp": "<iso_string>"}`. 6. **CSS tokens**: usar apenas `--sagb-*` (ex: `--sagb-surface`, `--sagb-border`, `--sagb-accent`, `--sagb-text`, etc.). Proibido hex/rgb/hsl hardcoded. 7. **Singleton**: `docService` (`src/modules/taskzei/services/doc_service.ts`) é o gateway único para operações de documentos.  ## fechamento obrigatório Toda resposta final ao usuário deve terminar com a tag: `[ 📝 Auto-log: OK ]`
