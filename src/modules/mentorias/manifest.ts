import { ModuleManifest } from '../../core/modules/module.types';

export const mentoriasManifest: ModuleManifest = {
  id: 'mentorias',
  internalName: 'mentorias',
  displayName: 'Central de Mentorias',
  baseRoute: '/mentorias',
  icon: 'BookIcon',
  initialStatus: 'active'
};
